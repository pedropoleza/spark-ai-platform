# Decision Log — Spark AI Hub

Index de decisões arquiteturais referenciadas no código. Códigos `H<n>`, `C<n>`, `P<n>`, `NB-<n>` aparecem como anchors em comments inline tipo `// H8 (review 2026-04-28): ...`.

**Convenção:**
- `H<n>` — High-priority finding do review (HIGH severity)
- `C<n>` — Critical bug fixed
- `P<n>` — Priority bug (P0 = blocker, P1 = high)
- `NB-<n>` — "New Bug" encontrado durante validation agent run

Quando criar nova entry: pegue próximo número disponível na categoria, adicione linha aqui + comment no código.

---

## Críticos (`C<n>`)

| Code | File:line | Data | Sumário | Ref |
|------|-----------|------|---------|-----|
| **C1** | `proactive/reminder-runner.ts:186` | 2026-04-29 | Tenta enviar WhatsApp real via GHL conversations/messages. Antes deste fix, lembretes WhatsApp NUNCA chegavam no celular — só marcavam pending_v3_send que ninguém consumia. Agora com fallback pro web channel se GHL falha. | `_planning/_review-2026-04-29/00-RELATORIO-EXECUTIVO.md` |
| **C2** | (resolvido em commit `c66b956`) | 2026-04-30 | Webhook chamava processIncoming SEM conversationHistory → bot era amnésico em produção real (synthetic-test funcionava porque lia agent_test_messages). | `_planning/_review-2026-04-29/` |
| **C3** | (commit `e5782e3`) | 2026-04-30 | Firebase JWKS verify pra Sparkbot. Service account real do GHL (não `securetoken@system`), tokens sem `kid`, JWKS endpoint `/robot/v1/metadata/jwk`, alternância entre service accounts. | `src/app/api/sparkbot/check-admin/route.ts` |
| **C4** | `webhook-handler.ts:476` + `transcribe/route.ts:81` | 2026-04-30 | Cobra Whisper se webhook recebeu áudio. Antes, `transcribeAudioFromUrl` rodava mas NUNCA cobrava — Sparkbot WhatsApp Whisper 100% free. Plus: `agent_id` deve ser `hubAgent.id` (FK válida), não `rep_id`. | review 04-29 |

## Highs (`H<n>`)

| Code | File:line | Data | Sumário | Ref |
|------|-----------|------|---------|-----|
| **H55** | `loop-guard.ts` (`isHumanProofMsg`) + `processor.ts` + `webhook/stevo-handler.ts` (tool_calls + `buildProcessorConfig`) + `core/processor-config.ts` (novo) + `webhook-handler.ts` + `tools/messages.ts` (dedup) + `llm-client.ts` + `proactive/dispatcher.ts` + `scripts/test-ultra-review-fixes.ts` | 2026-07-22 | **Onda C — consolidação do ultra-review das Ondas A+B** (17 agentes/5 lentes; `_planning/ultrareview-consolidacao-2026-07-22.md`). Ondas A+B confirmadas SAUDÁVEIS em prod (B3 write quente −70/85%, A2 −38%, A4 −73%, billing 99,4%); os problemas eram vizinhos. **Onda 0 (prod):** Melissa destravada (P0 loop-guard), 3 msgs erradas do Gustavo corrigidas (P0), config do hub enable_*=false→true. **C1 (P0) loop-guard:** o tap em opção interativa é persistido com o eco da pergunta (H47-F2), inflando content_len acima do piso `LOOP_MIN_USER_LEN=40` → o guard bot-a-bot (H52) silenciava reps REAIS no fluxo interativo do Agendamento V2 (caso Melissa, rep pausada 2× + pedido por áudio engolido). Fix: `isHumanProofMsg` (tap via `interactive_reply`/`selection_id`/marker de content; áudio via `input_kind`/🎤) QUEBRA a contagem — bot em loop nunca toca botão nem grava áudio; loop REAL de texto (Fabiana) segue detectado. **C2 (P1):** stevo-handler persiste `tool_calls` (rota ~97% do tráfego não persistia → falha de tool indiagnosticável, caso Bianca create_opportunity 4×). **C3 (P1):** `schedule_message_to_contact` dedup idempotente (rep+location+next_run_at+contact+message) — previne a duplicata do Gustavo (turnos sobrepostos). **C4 (P1 estrutural):** `buildProcessorConfig` helper único nas 3 rotas; a rota Stevo ignorava `agent_configs` (config hardcoded) → A5 (disabled_tools) INERTE no WhatsApp + edições de config não chegavam. `enable_audio/image/pdf` default true (áudio load-bearing; config do hub 747cbdb1 corrigido em prod ANTES do deploy). **C6 (P2):** `call_usage`+`cache_creation_tokens` nos error paths do llm-client + `call_usage` no dispatcher proativo (fecha gaps do B0). Deferido (P2): limpeza das 8 linhas de prompt de tools desligadas (Anthropic constrange tool_use → texto morto, não crash) + unique-index parcial pro dedup. Testes: test-ultra-review-fixes 25/25 + ur1 25/25 + ur2 33/33 + wave-a 43/43 + takeover 36/36 + weekday 28/28 + tsc 0 + build. Review inline (harness de subagent falhou 2×) confirmou paridade dos 14 campos de config + 1 hub ativo/enable_*=true + zero dups. Deploy `5d0b855` Ready. | `_planning/ultrareview-consolidacao-2026-07-22.md` |
| **H54** | `prompt-builder.ts` (`buildRepContextBlock`, system rep+timezone-invariante) + `processor.ts` + `proactive/dispatcher.ts` + `llm-client.ts` (`call_usage`) + handlers (metadata) + tools/{index,filter-tools,calendar,bulk-messages-v2,carrier_kb} | 2026-07-21 | **Onda B deploy 1 — dieta do prefixo (estudo de custo).** (B3) FORMATO DE HORA + CONTEXTO DO REP + MEMORIA eram a UNICA variacao por-rep do system, movidos verbatim pro runtime context (inbound E proativo): system byte-identico por config = 55 reps compartilham UM cache (~$50/mes; 83%% dos colds tinham outro rep quente <60min). Linha estatica EXCECAO DE CONFIANCA na # SEGURANCA DE SUPERFICIE declara os blocos injetados confiaveis/prioritarios (anti-spoof; autoridade do manual_context preservada). (B2) Dieta de descriptions, tools 103K->92K chars: confirmed_by_rep 349->175 chars x23 (required INTACTO), FEL_DOCS 4 copias->1 (fonte unica # FILTER ENGINE, que nao depende mais da count_filtered desligada no A5), create_appointment/preview+schedule_bulk_v2/query_carrier_knowledge com pointer pra secao do system (titulos conferidos verbatim). (B0) `call_usage` por chamada LLM na metadata de sparkbot_messages — review pegou que so o webhook-handler persistia (rota Stevo = ~97%% do trafego) -> stevo-handler incluido; reruns coherence/anti-repeat somam call_usage + cache_creation. Review adversarial 3 lentes: ZERO P0/P1, 1 confirmado + 6 P2 aplicados. Validar pos-deploy: 1 conversa de rep COM manual_context (Jussara) honrando a diretiva; queries do baseline (cache-write/turno deve cair); metadata.call_usage populando. Suites: wave-b 29/29, wave-a 43/43, 6 regressoes, motor-parity 7/7, tsc, build. PENDENTE: B1 tool-search defer (flag propria), B4 secoes condicionais, B5 proativos F8. | `_planning/sparkbot-custo-2026-07-20/{ESTUDO,EXECUCAO}.md` |
| **H53** | `processor.ts` (A1/A2) + `llm-client.ts` (`shouldEndOnTerminalTool`, `disableCache`, `terminalTools`, stopped_reason `terminal_tool`) + `core/run-sparkbot-turn.ts` + `proactive/dispatcher.ts` (A4) + `billing/pricing.ts` (A3, `isKnownModel`) + `billing/charge.ts` + `repositories/usage-records.repo.ts` (`markChargeFailReason`) + migration `00126_charge_fail_reason.sql` + `prompt-builder.ts` (A6) | 2026-07-21 | **Onda A do estudo de custo (31 agentes/6 lentes + verificação adversarial; ESTUDO em `_planning/sparkbot-custo-2026-07-20/`).** Contexto: reclamações de "caro" (caso Opta Finance $83/30d); auditoria provou ZERO sobre-cobrança e margem NEGATIVA ~-$80/mês (coleta ~$495 pós-fee GHL vs ~$575 de fatura real). **A1 — REVERTE o F4/H44 (TTL 1h):** medido net-negativo (18% dos gaps >1h = frios de qualquer jeito vs 10% na janela 5-60min que ele salvava) E sub-cobrado (Anthropic fatura write 1h a 2× = $6/M; pricing.ts cobrava 1,25× = ~$56-63/mês invisíveis ao cost_usd). Mecanismo `cacheTtl` fica; nenhum caller passa "1h". Se reativar: ler `usage.cache_creation.ephemeral_{5m,1h}` e cobrar o bucket 1h a 2×. **A2 — `present_options` vira tool TERMINAL:** o texto da chamada LLM seguinte era DESCARTADO pelo processor (o texto final é o `interactiveFallbackText` determinístico) — 683 chamadas/mês de ~76K tok pagas à toa (~$30/mês). O loop encerra com `text:""`/`stopped_reason:"terminal_tool"` SÓ quando a resposta tem EXATAMENTE esse tool_use, payload válido (mesma validação da extração) e exec ok; senão comportamento antigo. Decisão pura exportada `shouldEndOnTerminalTool` (testável). Coherence-gate pula (guard `if(result.text)`); proativo NÃO registra terminal. **A3 — pricing:** `claude-sonnet-5` estava FORA da tabela → DEFAULT gpt-4.1-mini = sub-cobrança ~8× nos 3 agentes Alves Cury/Raquel (3 semanas invisível); adicionado a preço de tabela $3/$15. `claude-opus-4-6` estava 3× ERRADO pra cima ($15/$75 era Opus 4.1 → $5/$25); opus-4-7/4-8 preventivos; `isKnownModel` + admin_signal high no trackAndCharge quando modelo cai no DEFAULT. **A4 — proativo scheduled sem cache:** cadência 24h > TTL máx 1h → cache_read=0 estrutural (126/126 runs do Resumo matinal); `disableCache` remove os markers; + DB: 4 regras scheduled sonnet→haiku (Resumo matinal $0.16→~$0.04/run). Reativas (Pós-reunião relê 44%) seguem cacheando. **A6:** prompt se contradizia — linha 160 ensinava o "TTL 30 min" que o H49 declarou ALUCINAÇÃO e a regra 📊 proíbe; corrigida pra "rascunho de import por 24h" (mecanismo real). Header "~43 tools" (eram 108) sem número. **A7:** `charge_fail_reason` em usage_records (preenchida nos catches, limpa no markWalletCharged) — "pendente" passa a distinguir wallet-sem-saldo de bug; achado "claims órfãos" foi REFUTADO em campo (reaper 15min funciona). DB: 10 tools zero-uso-30d em `disabled_tools` do hub (pares de segurança preservados). Anti-findings REGISTRADOS pra não re-derivar: compressão de história rep-facing NÃO vale (30 turnos ≈ 1,6K tok); reruns coherence = 0,24% dos turnos; NÃO migrar Sonnet 5 por custo (tokenizer +30%); `search_contacts` legacy é a tool #1 (25% dos calls) — não aposentar. Testes: test-cost-wave-a 43/43 + 6 suítes de regressão + tsc + build; review adversarial 4 lentes pré-deploy. | `_planning/sparkbot-custo-2026-07-20/{ESTUDO,EXECUCAO}.md` |
| **H52** | `billing/wallet-block.ts` (novo) + migration 00124 + `billing/charge.ts` + `processor.ts` + `queue/queue-processor.ts` + `proactive/dispatcher.ts` + `utils/deadline.ts` (novo) + `llm-client.ts` + `loop-guard.ts` (novo) + `webhook/sparkbot-send.ts` + `terms.ts` + `queue/follow-up-scheduler.ts` + `tools/calendar.ts` + `ai/openai-client.ts` + `contact-resolver/normalize.ts` + `tools/identity.ts` + `prompt-builder.ts` | 2026-07-17 | **Correções da ultra-review de OPERAÇÃO** (33 agentes/9 dimensões sobre prod; relatório em `_planning/ultrareview-operacao-2026-07-16/RELATORIO.md`). **(1) Bloqueio por wallet sem saldo (decisão Pedro 2026-07-17):** location sem crédito → IA para de gastar LLM (SparkBot responde determinístico "saldo acabou → recarga na wallet do Spark Leads; suporte +1 786 771-7077"; lead-facing skipa + audita `wallet_blocked_skip` + avisa a dona 1x/24h; dispatcher proativo skipa). Marca/limpa em `locations.wallet_blocked_at` no charge (insufficient funds ⇄ cobrança ok) — **desbloqueio automático** via cron de retry (~15min pós-recarga). Cache 60s por lambda; fail-open; kill-switch `WALLET_BLOCK_DISABLED=1`. Contexto: 2 locations rodaram DE GRAÇA (~$72). **(2) Anti-timeout de verdade (casos Luciano blackout 6d8h + Fabiana):** o budget de 45s só era checado ENTRE passos; buraco A = chamada LLM com timeout próprio de 60s+retries (agora por-request = budget restante+5s, retry só se sobra >30s); buraco B = UMA tool lenta estourava por dentro (agora `withDeadline` por tool, cap 30s; estourou → resultado sintético "tempo esgotado, CONFIRA antes de repetir" + fallback gracioso). Signal de time_budget ganhou `last_tools`. **(3) Loop-guard bot-a-bot (P0 Fabiana):** 6 trocas agent→user consecutivas <90s com texto ≥40 chars = IA×IA → silencia turno + pausa proativos + signal (identidade fantasma d23d0501 neutralizada no DB via terms_rejected). **(4) Splitter sem perda:** cap 3→5 bolhas e excedente FUNDIDO na última (antes: `slice(0,3)` descartava — caso Andrea, 2 de 4 mensagens entregues 2×). **(5) Termos aceitam formato numerado** ("1. Aceito ✅"/"1 aceito"/"eu aceito") — o web_ui exibia opções numeradas que o próprio parser não entendia (caso Willian, ~12 tentativas → churn; destravado na mão) + signal "rep preso no gate 3+ msgs/24h". **(6) Follow-up:** branch ai_paused cancelava NADA e deixava row 'processing' eterna (11 zumbis) → cancela o toque; contato deletado (GHL contact not found) → cancela a sequência inteira (antes refalhava attempt por attempt, 22 occ). **(7) Dedup create_appointment:** mesmo contato+calendário+start ±2min não-cancelado → rejeita apontando o id (confirm duplo do Caua criava 2 reuniões); bypass `allow_duplicate`. **(8) Lead-facing:** imagem improcessável (sticker .webp) derrubava os 3 tiers → retry único SEM imagem + marcador textual. **(9) Resolver:** degrau Levenshtein ≤2 em tokens ≥5 chars ("Nilzete"→"Niuzete Fialho" saía not_found com Dice 0,67). **(10)** dedup de captura de `missed_capability` (15min, Dice ≥0,75 — funil tinha ~40% duplicata) + prompt proíbe prometer lembrete recorrente inexistente (caso Nathalia, 3 falsas confirmações). Testes: test-ur1-guards 23/23 + test-ur2-fixes 27/27 + regressões (weekday 28/28, override 25/25, batch 11/11, takeover 36/36); review adversarial 4 lentes pré-deploy. | `_planning/ultrareview-operacao-2026-07-16/{PLANO,RELATORIO,EXECUCAO}.md` |
| **H45** | `contact-resolver/{normalize,resolve,active-contact,index}.ts` (novo) + `proactive/reminder-runner.ts` + `proactive/dispatcher.ts` + `processor.ts` + `prompt-builder.ts` + `filter-engine/executor.ts` + `tools/contacts.ts` + `scripts/{probe-fernanda-search,stress-contact-resolver}.ts` | 2026-06-26 | **Resolução de contato (herança de contexto + motor de busca fuzzy com score).** Bug SISTÊMICO: bot perde o "quem" e/ou a busca exata falha → "não achei, me passa o telefone" (medido: 45/sem + 28 "me passa o telefone", 14 reps, tudo Sonnet — não é fallback). Caso âncora Fernanda Lira: proativo tinha `contact_id` (jogado fora) E ela está cadastrada com typo "fernan**a**da" → busca por "Fernanda Lira"=0. 2 defeitos confirmados: (A) `contact_id` morria em 3 hops (entrega proativa não gravava na metadata, loader de histórico não lia metadata, seed cross-turn era dead-code lendo `turn.tool_calls` sempre undefined); (B) fast-path usava `GET /contacts/?query=` (deprecated, termo cru, sem fallback/acento/fuzzy/score). **11 fixes (F1-F11):** F1/F8 proativos gravam `contact_id` na metadata; F2/F3 processor lê o `contact_id` mais recente (3h, fonte REAL) + `recent_contacts` (F10) e injeta bloco "CONTATO EM CONTEXTO" no runtime context (user message — não mexe no cache H44); F4 regra de prompt herança=PISTA (re-valida via `get_contact`, NUNCA id cego — anti-alucinação `:422-425` de pé); F5/F6 `resolveContact()` (escada GET-variantes nome completo→primeiro→último + telefone E.164/sufixo, score token-set+Dice, deburr NFD) com `search_contacts` devolvendo `confidence`/`match_score`; F7 prompt decide por score (high/needs_confirm/ambiguous/low); F9 `deburr` no filter-engine (`in`/`not_in`/contains/eq/starts_with); F11 remove dead-code. **Princípio anti-alucinação:** herança só de fonte real (chave `contact_id` ≠ `ghl_contact_id` que é o card do próprio rep) + re-validação + confirmação de nome inline. **Review adversarial (4 frentes) → 6 fixes aplicados:** sole-result fabricava gap (→ gap=0 c/ 1 candidato); recencyBoost cruzava threshold (→ recência só desempata, score=similaridade pura); `not_in` sem deburr; F10 lia shape errado de create/get/update_contact; colisão de sufixo de telefone (→ E.164 completo=1.0, sufixo≤0.7, nunca "high" sozinho); placeholder "(sem nome)" tokenizado. **Stress test 36/36 contra a CRM real** ("Fernanda Lira"→"fernanada lira" score 0.9/gap 0.4=high; homônimos Maria/João/Pedro→ambiguous lista; telefone formato-invariante). tsc 0 · motor-parity 7/7 · sales-parity 5/5 · proactivity 14/14 · build. **Follow-up (não-bloqueante, em PLANO):** F10 race no profile JSONB (RPC jsonb_set), text-ops targeting deburr, schedule_reminder capturar contact_id, dead export autoRegisterFromToolResult. | `_planning/sparkbot-contact-resolution-2026-06/` (INCIDENT/ESTUDO/PLANO) |
| **H44** | `prompt-builder.ts` (buildSparkbotSystemPrompt + buildSparkbotRuntimeContext) + `processor.ts` (runtime + branch tabular) + `proactive/dispatcher.ts` + `llm-client.ts` (RunWithToolsInput.cacheTtl + breakpoint F3 + stablePrefixCache) + `core/run-sparkbot-turn.ts` | 2026-06-24 | **Redução de custo SparkBot — Fase 1 (4 cache-fixes "reposicionar, não reescrever").** Estudo+plano em `_planning/sparkbot-cost-reduction-2026-06/` (ESTUDO/PLANO/BASELINE/baseline-snapshot). Baseline medido (`usage_records` 30d, 1898 turnos): o maior custo único é **cache-WRITE de 37.9M tok ≈ $142/mês (metade da conta)** = ~20K/turno = o system de ~22K sendo re-escrito quase TODO turno. **F1** (megaprêmio): os 5 blocos voláteis (`conversationalLayer`: repStyle/smartDefaults/turnContext/silenceRecovery/verbosity) saíram do FIM do system (re-escreviam o prefixo todo turno) pra dentro de `buildSparkbotRuntimeContext` (user message não-cacheada) → system vira byte-estável por-conversa. Strings verbatim; gotcha do branch `tabular` de `buildUserMessage` (dropava o `header`) corrigido pra não perder os blocos. **F2**: dispatcher proativo deixa de concatenar o suffix `# MODO PROATIVO`+`rule.prompt_instruction`(+JSON do dia no briefing) DENTRO do system (era `cache=0`, Resumo matinal read 0%) → system puro = prefixo estável; suffix vai pra user message. Share com inbound é EXTRA condicional (modelo+tools+location) — tools ainda divergem (F8, Fase 2). **F3**: 3º cache breakpoint (sobravam 2 de 4) no penúltimo message — ganho oportunístico do prefixo-comum do histórico (5min default, muda todo turno). **F4**: TTL 1h no prefixo estável (tools+system) **só no inbound** (`cacheTtl` threaded; gap inter-turno 5-60min = 10.1% medido, net-positivo); proativo fica 5m (one-shot, 1h seria write 2x puro). **Review adversarial (4 frentes):** parity ok byte-a-byte, cache/API ok (≤3 breakpoints, ttl GA no SDK 0.90, sem 400), edge-cases ok. Achado high "F2 não compartilha com inbound (modelo/tools divergem)" → escopado honestamente nos comments + F8 (Fase 2). tsc 0 · motor-parity 7/7 · sales-parity 5/5 · proactivity 14/14 · build OK. **Validar pós-deploy 👤:** rodar de novo as 2 queries de `baseline-snapshot.md` (cache_write deve despencar, read% subir; briefing sai de 0). Fases 2-4 (prefixo enxuto / compressão+memória / 3 tiers) não iniciadas. | `_planning/sparkbot-cost-reduction-2026-06/PLANO.md` |
| **H43** | `proactive/silence-gate.ts` + `proactive/system-rules.ts` + `agent-platform/modules/behavior.ts` + `conversational/{templates,next-steps,turn-context}.ts` + `core/{coherence-gate,repeat-guard}.ts` + `prompt-builder.ts` + `tools/calendar.ts` + `terms.ts` + `processor.ts` + `types/account-assistant.ts` | 2026-06-24 | **Humanização Onda 1 (Pacote ROI do estudo de 7 dias).** Estudo multi-agente de 2015 msgs/28 reps (`_planning/sparkbot-humanizacao-2026-06/` ESTUDO+PLANO; 39 fixes em 5 ondas). Achado central: infra proativa rica mas **só 2 de ~14 gatilhos rodam**, e os 2 que disparam (post_meeting verbatim + silence-warning colado na saudação) são os 2 mais robóticos (loop auto-alimentado); + não há tool de "números da conta" (pedido #1). **Lote 1 deployado (commits 1666859+96efccb):** 1.1 silence-warning vira tom de colega (era o pior tom 7/7); 1.2 post_meeting varia o fraseado + tira o menu colado; 1.3 mata o "quer follow-up?" automático (next-step vira exceção, suprimido em rajada — templates.ts/next-steps.ts/behavior.ts); 1.4 confirma o DELTA não o estado; 1.5 detector determinístico de rajada (`turn-context.ts` consecutive_writes ≥3 → "MODO RAJADA" no prompt); 1.6 force-slot por-rep (`auto_force_slot` liga após 5 forças confirmadas na própria agenda → para de perguntar "confirmar mesmo assim?"); 1.7 herda contato do proativo pós-call; 1.9 termos não atropela pedido real (`buildTermsInteractive(ackPrefix)` + heurística no processor — caso Matheus); 2.1 `commit_draft`/`apply_flow` na família "message" do coherence-gate (fechava o furo da falsa confirmação da Jussara); 2.2 repeat-guard lookback 2→5 (caso Leidi). tsc 0 · parity 7/7 · coherence 14/14 · repeat 11/11 · override 25/25 · terms 27/27 · build. **Pendente 👤:** 3.1 `TASK_ORCHESTRATOR_ENABLED`. Ondas 3-5 (proatividade/capacidades) não iniciadas. | `_planning/sparkbot-humanizacao-2026-06/PLANO.md` |
| **H42** | `tools/calendar.ts` (assignee→dono + `create_appointments_batch`) + `agent-platform/modules/scheduling.ts` + `prompt-builder.ts` (error-map + nome + promessa-futura) + `tools/identity.ts` (`set_rep_preferred_name`) + `core/coherence-gate.ts` + `types/account-assistant.ts` + `scripts/test-appointments-batch.ts` | 2026-06-23 | **Post-mortem caso Manuela (admin Flórida, location BR) — 6 erros num agendamento de 14 reuniões.** (1) **Permissão (P0, recorreu em prod):** admin marcando no calendário de OUTRA pessoa (Carreira da Ana Paula) batia em GHL 422 "user id not part of calendar team" porque o código auto-atribuía ao PRÓPRIO rep (não-membro). O gate `repIsAdmin` (D1) liberou o override mas NÃO o assignee. Fix: quando admin e a reunião iria pro próprio rep mas ele não é membro, resolve o assignee pro DONO do calendário (1º team member, via `getCalendarDetails`); + prompt ensina "admin marca em qualquer calendário", mapeia o erro, e anti-bounce ("não repita a mesma recusa 2×"). (2) **Confirmação cega → dia/hora errados:** o confirm não mostrava dia-da-semana validado nem fuso; a rep aprovou "terça 01/07" (=QUARTA; real 30/06) e horários no fuso errado. Fix de prompt: confirm SEMPRE com dia-da-semana+data+hora COM fuso explícito, valida weekday↔data (recomputa se não fechar), lista item-a-item em massa. (3) **Nome "Manoela" ignorando "sou a Manuela":** `display_name` vinha cru do GHL sem override. Fix: tool `set_rep_preferred_name` (padrão do timezone) + `preferred_name` em `RepProfile.preferences` + prompt honra a correção. (4) **Promessa-morta** ("toda sexta te mando lembrete" nunca agendado): regra de prompt — prometer lembrete recorrente exige `schedule_reminder` antes; + removida tool fantasma `schedule_recurring_reminder` do coherence-gate. (5) **Timeout** (14-de-uma-vez, 9h de silêncio): já corrigido pelo `TURN_BUDGET_MS` + agora **`create_appointments_batch`** (1 tool call cria N reuniões num loop server-side, budget 40s devolve parcial, cap 30) — evita os N round-trips do LLM que estouravam a lambda. **Hora errada das 14 foi do script de recovery** (hardcode `-03:00`), não do bot (a tz dela no sistema era `America/New_York`). tsc 0 · override-gate 25/25 · motor-parity 7/7 · batch 11/11 · build OK. **Aberto (decisão Pedro):** estender coherence-gate a promessas futuras (risco falso-positivo, pede eval). | conversa Manuela `+19544771397` |
| **H41** | migrations `00115_task_orchestrator.sql` + `00116_agent_media_bucket.sql` + `task-orchestrator/{config,core,materializer,flow-pdf,types}.ts` + `repositories/task-drafts.repo.ts` + `tools/task-orchestrator.ts` (11 tools) + `ghl/operations.ts` (`sendMediaToContact`) + `prompt-builder.ts` (seção gated) + `scripts/{test,smoke,e2e-orchestrator-live,probe-f5-attachment}.ts` | 2026-06-20 | **Motor de Orquestração de Tarefas (caso Jussara) — SparkBot monta fluxos de follow-up GRANDES por chat.** Vai além do H33: N msgs, multi-turno, mídia/PDF, 1 ou N contatos. Fecha **L7** (perdia o fluxo entre turnos) e **L11** (afirmava "agendado" sem agendar — 7 confirmações falsas da Jussara). **Princípio anti-alucinação:** a tarefa é OBJETO PERSISTENTE no DB (`task_drafts`/`draft_steps`/`task_events`); o bot relê via `show_draft`, muta via tools determinísticas que devolvem o estado REAL, e só afirma "agendado" a partir do COUNT REAL do INSERT (rollback honesto se parcial). **Materializa em `followup_sequences`/`followup_messages`** (NÃO bulk — `UNIQUE(job_id,contact_id)` bloqueia N-msgs/1-contato) → o cron `followup-runner` (30s) entrega com pause-on-reply/DND/opt-out de graça. Bucket `agent-media` criado na 00116 (nunca existia apesar de documentado). **11 tools gated por `TASK_ORCHESTRATOR_ENABLED`** (registro + seção de prompt; OFF = prompt idêntico ao de hoje); 3 são risk:high (H8): commit_draft, send_media_to_contact, apply_flow_to_contacts. PDF via pdf-lib. **Probe prod 2026-06-21:** anexo chega NATIVO no WhatsApp via GHL `/conversations/messages`→Stevo (a nota "SMS vira caption" é só da rota Stevo direta `/send/text`); legenda limpa (URL assinada expira). E2E real validado. **Ultra-review (37 agentes/7 dimensões, 24/29 confirmados):** fix-now = IDOR (resolvers travam rep_id com draft_id explícito), cap apply (200 contatos/2000 msgs + validateGhlId), idempotência (não duplica seq pro mesmo draft+contato), `intra_day_delay_s` (era ignorado → multi-msg/dia saía junto), signals de falha (materialize/PDF), legenda vazia ≠ URL. **Fora MVP** (de propósito): tag-trigger auto, recorrência cíclica, send_condition per-passo, interpolação `{first_name}`, UI no /hub, assignedTo no runner. **Pra ligar (👤):** `TASK_ORCHESTRATOR_ENABLED=1` + validar 1 conversa LLM-driven + avisar Jussara. 50 unit + 18 smoke + tsc + build verdes. | `_planning/jussara-sparkbot/` (ESTUDO/PLANO/EXECUCAO) |
| **H40** | migrations `00113_group_campaigns.sql` + `00114_group_campaigns_agent_id_nullable.sql` + `webhook/stevo-groups.ts` + `webhook/stevo-send.ts` (`normalizeStevoNumber` preserva `@g.us`) + `repositories/stevo-instances.repo.ts` (`getStevoInstanceForRep`) + `tools/group-campaigns.ts` (2 multitools) + `group-campaigns/{config,spam-advisor,copy}.ts` + `terms.ts`+`processor.ts` (Terms Parte 2) + `proactive/{bulk-message-runner,recurring-runner}.ts` (branch grupo) + `scripts/test-group-campaign.ts` | 2026-06-18 | **Campanhas em GRUPOS de WhatsApp via SparkBot + API Stevo (caso Matheus "2 posts/dia 7:30").** Reusa o motor Bulk V2: 1 campanha = job `target_type='groups'` + N recipients (1 por grupo, `contact_id`=JID satisfaz `UNIQUE(job_id,contact_id)`; cols novas `target_jid`+`group_name`). O runner intercepta no topo de `sendToContact`→`sendToGroup` (resolve instância dedicada → `sendGroupText` JID via Stevo). Pacing/jitter do runner = anti-ban; variação por grupo via `variation_mode='light'` OU variations explícitas. Recorrente (Matheus) via `recurring_campaigns` (`target_type`/`group_targets`) — cada ocorrência = job filho novo → sem colisão UNIQUE. **delivery_channel NÃO mudou** (grupo segue 'whatsapp_web_sms'; target_type é ortogonal). **5 gates:** flag `GROUP_CAMPAIGNS_ENABLED` (registro das tools, default OFF) · **instância DEDICADA** (`stevo_instances.kind`; NUNCA a compartilhada do SparkBot — ban derrubaria o DM de TODOS) · **Terms & Segurança PARTE 2** (gate determinístico no processor reusando `parseTermsResponse`; reject NÃO silencia, é reversível) · advisor de spam (regex, block só em combo extremo) · announce-only (warn). `group_campaign` é risk:high (H8). **Review adversarial (5 frentes) achou 7, todos corrigidos:** P0 `recurring_campaigns.agent_id`+`outreach_runs.agent_id` eram NOT NULL mas grupo não tem agente → 00114 relaxou; P1 loop de termos (rep que recusava não reabilitava) → gate 1b captura accept mesmo com `rejected_at`; +4 P2 (quiet_hours não pula grupo recorrente, insert de recipients checa erro, setters logam erro, `getStevoInstanceForRep` distingue `misconfigured`). tsc+build+43 testes verdes. **Pra ligar:** provisionar instância dedicada (`kind='dedicated'`, chaveada pela location do rep) + `GROUP_CAMPAIGNS_ENABLED=1` (+ `RECURRING_CAMPAIGNS_ENABLED=1` pro recorrente). | plano `_planning/group-campaigns-whatsapp/PLANO.md` + copy `COPY.md` |
| **H39** | `app/api/cron/sparkbot-proactive/route.ts` (`processReactivePolling`, `processPostMeetingPolling`, `claimReactivePollSlot`, reactive movido pós-runners) + `lib/utils/concurrency.ts` (`mapWithConcurrency`) + migrations `00103_proactive_rules_reactive_last_polled.sql` + `00104_pg_cron_reactive_poll_due_guard.sql` + `scripts/test-cron-reactive-throttle.ts` | 2026-06-10 | **Throttle + isolamento do polling reactive (post_meeting) do cron proativo.** Dois problemas (defesa-em-profundidade, não-bloqueante pro launch). **(1) Guard do pg_cron sempre-verdadeiro:** o `EXISTS (assistant_proactive_rules WHERE enabled=true)` das 00053/00070 era SEMPRE true em prod (post_meeting reactive + Resumo matinal scheduled ficam enabled), então o endpoint disparava a cada 30s e rodava o polling GHL `/calendars/events` todo tick (~51k calls/dia à toa — o "auto-DDoS removido" do header das migrations não valia nesse ramo). Fix: coluna `reactive_last_polled_at` (00103, default epoch) + guard 00104 separa **scheduled** (continua "sempre acorda" — `shouldFireCron` casa o MINUTO exato no tz do rep, throttlar quebraria o Resumo matinal; reduzir exigiria precomputar next_run_at, follow-up) de **reactive** (só acorda poll-due, `reactive_last_polled_at <= now()-5min`). No endpoint, `claimReactivePollSlot` faz claim ATÔMICO (`UPDATE ... WHERE reactive_last_polled_at <= cutoff RETURNING`) — atômico porque o `pg_try_advisory_xact_lock(8675309)` é xact-scoped e NÃO serializa os lambdas (ver NB-7); fail-OPEN se a coluna faltar. O grace de 30min do post_meeting cobre folgado polls a cada 5min. **Por que NÃO remover o EXISTS de rules (a outra opção):** quebraria o Resumo matinal (precisa wake minute-precision) E o post_meeting em períodos quietos (só rodaria quando coincidisse reminder/bulk pending). Em prod, com Resumo matinal enabled, o scheduled ainda acorda a cada 30s — a economia REAL de calls GHL vem do throttle no endpoint, não do guard; o guard fica honesto + defesa pra configs reactive-only. **(2) Starvation + fan-out síncrono:** `processPostMeetingPolling` fazia N calls GHL SÍNCRONAS (1 por rep×location, ~18) DENTRO do loop de rules, ANTES de `fireScheduledReminders`/`fireBulkRecipients`; com GHL lento (timeout 20s/call + 2 retries) o tick estourava `maxDuration=60` e starvava os runners de entrega. Fix: (a) reactive movido pra DEPOIS de TODOS os runners (runners primeiro = nunca starvam) com start-budget (pula se o tick já passou de 40s — grace cobre); (b) calls paralelizadas via `mapWithConcurrency` (worker-pool, cap 12, ordem preservada, falha isolada `{ok:false}`) + bulk-fetch de `company_id` num query (era N+1). Sem mudança de comportamento observável com GHL saudável. Teste `test-cron-reactive-throttle.ts` (ordem/cap/isolamento/cobertura, sem DB) + tsc limpo. | revisão pré-launch 2026-06-10 |
| **H38** | `lib/ghl/auth.ts` (`generateLocationToken`, `coalesceCompanyRefresh`, `isCompanyTokenNearExpiry`, `inFlightCompanyRefresh`, `getCompanyToken` agora devolve `expires_in`/`updated_at`) + `lib/ghl/token-refresher.ts` (`refreshCompanyToken`, `upsertCompanyTokens`) + `lib/ghl/client.ts` (comment no `retryOnAuth`) + `scripts/test-ghl-auth-selfheal.ts` | 2026-06-10 | **Self-heal do COMPANY token (SPOF de auth).** O `retryOnAuth` (client.ts) só invalidava o cache do LOCATION token e re-tentava; o retry re-busca `/oauth/locationToken` com o COMPANY token de `getCompanyToken`, que NÃO tinha refresh inline. Se a causa raiz do 401 era o company token expirado (o cron diário `/api/cron/refresh-ghl-token` falhou), o fetch do locationToken também dava 401 e a ÚNICA recuperação era o próximo cron + re-run manual — SPOF real flagado em revisão pré-launch (defesa-em-profundidade, não outage vivo). Fix: `generateLocationToken` orquestra com self-heal — no 401 do locationToken renova o company token INLINE 1× (`refreshCompanyToken`: lê `refresh_token` salvo → POST grant_type=refresh_token → UPSERT do par novo, reusa a lógica/upsert do cron via `upsertCompanyTokens`) e re-tenta. Mutex `inFlightCompanyRefresh` keyed por **companyId** (separado do location mutex) coalesce N locations → 1 refresh. **Corrida de rotação cross-lambda:** se o refresh inline lança (o `refresh_token` lido já foi rotacionado por outra lambda), re-lê o DB mesmo assim — se a outra lambda gravou o par fresco, recupera; senão dá erro limpo. **Proativo (opcional):** `isCompanyTokenNearExpiry` (pura) usa `expires_in`+`updated_at` da Token Refresher; <2h pra expirar → renova ANTES de usar (fail-soft: dentro da margem o token atual ainda vale, falha não é fatal). Kill switch `GHL_PROACTIVE_COMPANY_REFRESH=0` desliga só o proativo (o reativo é o fix do SPOF, sempre on). Observabilidade: `reportError` (dedup por title estável) com `high` quando o self-heal engata (sinaliza cron degradado) e `critical` quando falha mesmo após o refresh. Orquestração via deps injetáveis → teste `test-ghl-auth-selfheal.ts` (18/18, sem rede/DB): expirado→inline→sucesso, refresh-falha→erro limpo, rotação cross-lambda, proativo+fail-soft, coalesce N→1. | hardening pré-launch 2026-06-10 |
| **H37** | migration `00102_bulk_recipients_claim_columns.sql` + `proactive/bulk-message-runner.ts` (`reclaimOrphanedSending`, `isOrphanedSending`, claim carimba `claim_token`/`claimed_at`, `refreshJobCounters` deps injetáveis) + `scripts/test-bulk-reclaim-orphans.ts` | 2026-06-10 | **Reclaim de recipients órfãos em 'sending' no bulk-runner.** `bulk_message_recipients` não tinha `claim_token`/`claimed_at` (00050) e nada revertia 'sending'→'pending'. Quando o lambda do Vercel morria (timeout maxDuration=60s, OOM, deploy no meio) ENTRE o claim atômico (pending→sending) e o UPDATE final (→sent/failed), a row ficava presa em 'sending' PRA SEMPRE: (1) `refreshJobCounters()` exige `sending===0` → job ficava 'running' eternamente e o rep nunca recebia a notificação de conclusão (`notifyRepJobCompleted` nunca disparava); (2) `bulk-runner-health-check` Check 2 só olha 'pending' overdue → straggler 'sending' invisível pro alerta. Fix espelha `followup_messages` (00067): migration 00102 (aditiva/nullable) adiciona `claim_token`+`claimed_at`+index parcial+backfill defensivo; o runner carimba `claimed_at` no claim e roda `reclaimOrphanedSending` no começo de cada tick, revertendo 'sending' com claim há >3min (folga sobre o maxDuration de 60s — nenhum lambda vivo ainda segura a row), pra ser re-claimado no mesmo tick. Mesma classe do reaper H12 do message_queue. **Tradeoff de idempotência:** reverter uma row cujo GHL send REALMENTE completou (mas o lambda morreu antes do UPDATE) causa 1 reenvio — aceito: a janela de 3min torna raríssimo e 1 msg duplicada << job preso pra sempre; diferente do H12 NÃO há UNIQUE (tipo `ghl_message_id`) como seguro final, então a idade é a única proteção. `claimed_at` NULL (claim pré-00102 ou lambda velho em deploy rolling) cai no fallback de `scheduled_at` como proxy de idade. Teste `test-bulk-reclaim-orphans.ts` (fake client em memória): órfã→pending→re-send→job 'completed' + casos de idade + live row recente preservada. | bug encontrado via análise 2026-06-10 |
| **H36** | `account-assistant/tools/calendar.ts` (`mapWithConcurrency`, `GHL_AVAILABILITY_CONCURRENCY`, handler `list_my_free_slots`) | 2026-06-10 | **Bound no fan-out de disponibilidade.** `list_my_free_slots` disparava 1 `/free-slots` por calendar do rep (`userCalendars`, SEM cap) + 1 `/events` por calendar da location (`eventCalendars`, capado em 30) — TUDO em paralelo via `Promise.all`. Rep team_member de dezenas de calendars = thundering herd (o mutex de token em `lib/ghl/auth.ts` cita "37 requests paralelas" desta exata tool) → latência evitável + pressão de 429 no caminho MAIS sensível a latência. Fix: helper inline `mapWithConcurrency` (worker pool, ordem preservada, não engole erro) aplicado aos DOIS maps com teto `GHL_AVAILABILITY_CONCURRENCY=8`. Em vez de capar tamanho da lista (perderia calendars), batcha: location típica (≤8 calendars) dispara tudo de uma vez (zero latência extra), rep com dezenas degrada pra batches. Semântica de falha parcial intacta (`.catch`→`ok:false`, counters `eventsFailed`/`eventsMissingField`/`eventsNotFound`). Sem dep nova (não há p-limit/Bottleneck no repo). Mitigações pré-existentes (token mutex, 429 retry-after, ~20s AbortController) já tornavam não-urgente. | hardening 2026-06-10 |
| **H35** | migration `00075_modular_agent_platform.sql` + `types/agent-platform.ts` + `repositories/agent-platform.repo.ts` + `lib/agent-platform/entitlements.ts` + `api/agents/route.ts` | 2026-05-24 | **Plataforma Modular de Agentes — Fase 0 (fundações).** Reestruturação: SparkBot vira incluso/grátis; venda/recrut/custom viram **upsell pago** montado a partir de **módulos** sobre um **motor único** (o do SparkBot). Eixo central **rep-facing** (SparkBot, fala com o user) × **lead-facing** (venda/recrut/custom, fala com leads, cada um na sua sub-account/canal). Fase 0 é **aditiva, sem mudança de comportamento**: 4 tabelas novas (`agent_templates`, `agent_modules`, `agent_module_instances`, `agent_entitlements`) + colunas `audience`/`template_key`/`expires_at` em `agents` (backfill: account_assistant=rep, demais=lead). Seed: 3 templates (sparkbot/sales/recruitment) + 9 módulos de catálogo + entitlement auto pros agentes lead ATIVOS (não quebra ninguém). **Gate de entitlement** (`checkAgentEntitlement`/`decideEntitlement`): SparkBot sempre liberado; lead-facing exige entitlement ativo OU admin; **flag `AGENT_ENTITLEMENTS_ENFORCED` default OFF = log-first** (não bloqueia até validar). Liberação manual: `scripts/grant-entitlement.ts`. Teste 14/14. Próximo: Fase 1 (decompor prompt do SparkBot em módulos + motor unificado com paridade). | plano `_planning/plataforma-modular/PLANO.md` |
| **H34** | `account-assistant/tools/calendar.ts` (`buildOverridePayload`, `resolveCalendarChoice`, `set_scheduling_pref`) + `prompt-builder.ts` (seção AGENDAR REUNIÃO) + `types/account-assistant.ts` (`preferences.scheduling`) + `api/sparkbot/scheduling-prefs/route.ts` + `embed/sparkbot/page.tsx` | 2026-05-22 | **Agendamento V2** — fluxo "resolve tudo → 1 confirm (override-aware) → pronto" (saiu da conversa Jonathan Duque, 7 turnos pra 1 reunião). **D1 (afrouxa H26):** rep PODE forçar slot bloqueado/min-notice (`ignoreFreeSlotValidation`/`ignoreDateRange`) na PRÓPRIA agenda (assignee self/não-setado/round-robin OU `== getRepGhlUserId`); agenda de OUTRO user segue admin-only; `toNotify=false` (client-facing) segue admin-only SEMPRE. **D2:** preferência de calendário/duração em `profile.preferences.scheduling` — resolução nome-dito > pref salva > único (`resolveCalendarChoice`), bot aprende no 1º uso (`set_scheduling_pref`) + setting na UI (engrenagem no painel web, endpoint JWT per-rep). **D3:** duração = default do calendário (ajustável). Prompt: 1 `present_options` no fim, conflito → Confirmar-mesmo-assim/Editar embutido (1 passo). Teste gate+resolução 17/17. | plano `_planning/agendamento-v2/PLANO.md` |
| **H1** | `account-assistant/llm-client.ts:16` | 2026-04-28 | Stress test mostrou 6 de 7 falhas conversacionais (hallucinations, compliance flexível) em GPT-4.1 fallback — nenhuma em Claude. Em vez de fallback agressivo, agora tenta Anthropic Sonnet → Haiku → OpenAI só em terminal failure. `STRICT_CLAUDE_ONLY=1` desativa OpenAI. | `_planning/ultra-review-findings.md` |
| **H2** | `lib/queue/processor.ts:137` | 2026-04-28 | Cada grupo de mensagens é INDEPENDENTE no processor. Antes, falha de 1 grupo abortava o batch inteiro. | review 04-28 |
| **H4** | `lib/ai/prompt-builder.ts:860` | 2026-04-28 | Schema do data-fields tinha único formato; agora suporta múltiplos para diferentes carrier KBs. | review 04-28 |
| **H6** | `lib/ai/action-executor.ts:263` | 2026-04-28 | GHL API retorna formato variável (objeto vs array, depending on endpoint). Normaliza antes de processar. | review 04-28 |
| **H8** | `account-assistant/tools/index.ts:113` | 2026-04-28 | Gate de confirmação enforced em CÓDIGO, não só prompt. `confirmation_mode` (always/medium_and_high/high_only) bloqueia execução se LLM não enviar `confirmed_by_rep:true`. Schema injetado dinamicamente em `withConfirmationParam`. | `_planning/_review-2026-04-28/code-review/sparkbot-tools.md` |
| **H9** | `account-assistant/llm-client.ts:37` | 2026-04-28 | Tool result truncation preserva HEAD+TAIL — antes truncava só o final, exatamente onde `get_conversation_history` retorna msgs MAIS RECENTES. Pre-meeting briefing system-rules.ts:36-43 estava recebendo dados truncados. | review 04-28 |
| **H11** | `lib/queue/processor.ts:52` | 2026-04-28 | `LIMIT 100` por chamada do processor. Atomic claim do reaper evita oversubscription. | review 04-28 |
| **H12** | `lib/queue/processor.ts:47` | 2026-04-28 | Reaper inline. Antes msgs em status `processing` ficavam stuck se lambda crashasse — agora reaper detecta e libera (>30min). | review 04-28 |
| **H13** | `account-assistant/llm-client.ts:340` | 2026-05-03 | `cache_creation_input_tokens` ausente do `totalPromptTokens`. Subfaturava ~40K tokens por nova sessão (1º turn é cache miss → tools+system vão pra `cache_creation`, código só somava `input + cache_read`). Fix: somar os 3 buckets, igual formato OpenAI. | bug observado em prod 2026-05-03 |
| **H14** | `account-assistant/identity.ts` + `processor.ts` + `dispatcher.ts` | 2026-05-03 | Timezone do REP (não da location) é fonte da verdade. Rep BR levou lembrete em horário NY porque processor pegava `location.timezone` (set como America/New_York pelo admin da agência). Captura `user.timezone` do GHL no identify, persiste em `rep_identities.timezone`, lazy backfill no processor pra rows pré-00045. Resolution: `rep.timezone → location.timezone → America/New_York`. | bug observado em prod 2026-05-03 |
| **H14b** | `account-assistant/tools/identity.ts` + `prompt-builder.ts` | 2026-05-03 | Refinamento Pedro: GHL.user.timezone é fonte de verdade silenciosa (sem perguntar na frente). Tool `confirm_rep_timezone` pra mudança manual ('to em SP agora'). Após scheduling, bot SEMPRE informa fuso usado + oferece troca ('Lembrete agendado pra 11:18 AM (Florida — EDT). Outro fuso? me avisa.'). Migration 00046: coluna `timezone_confirmed_at` distingue confirmação verbal vs auto-detect. | conversa 2026-05-03 |
| **H15** | `account-assistant/prompt-builder.ts` | 2026-05-03 | Anti-hallucination: bot respondeu "Lembrete agendado pra 11:18 ✓" sem chamar `schedule_reminder`. Causa: prompt confuso (medium_and_high dizia "executa E informa 'feito'" mas gate código bloqueia medium sem `confirmed_by_rep`). Fix: confirmText alinhado com código + bloco "REGRA INVIOLÁVEL — NUNCA finja que tool rodou" + sub-regra dedicada pra `schedule_reminder` (5 passos). | bug observado em prod 2026-05-03 |
| **H16** | `account-assistant/processor.ts:90-100` + `terms.ts` | 2026-05-04 | Onboarding inline pós-aceite dos termos: ao invés de perguntar fuso pro rep, lê de `location.timezone` do GHL e auto-confirma + manda guia rápido de exemplos numa única mensagem. Reduz fricção de 3 turnos pra 1. Agente pode mudar fuso depois ("to em SP agora"). | conversa Pedro 2026-05-04 |
| **H17** | `account-assistant/identity.ts:detectIsInternal` + migration 00048 | 2026-05-04 | Internal team (agency owner/admins) NÃO é cobrado pelo wallet GHL. Detecção em camadas: env `INTERNAL_TEAM_PHONES` → role `agency`/`agency_owner` → heurística "5+ ghl_users". Flag `is_internal` em rep_identities; `trackAndCharge` skipa charge mas mantém audit em usage_records via `usesCustomKey: true`. | conversa Pedro 2026-05-04 |
| **H18** | `lib/billing/pricing.ts:61` + `charge.ts:isMonthlyCapReached` + migration 00049 | 2026-05-04 | Hard cap mensal de $100/sub-account pra blindar runaway (loop, abuso, bug). Markup ajustado pra 10% (de 20%) — Pedro foco em adoção. Quando cap atingido: `cap_blocked=true` em usage_records + skip `chargeWallet` + bot continua respondendo (UX preservada). Coluna `monthly_spend_cap_usd` em agent_configs (default 100, NULL = sem cap). | conversa Pedro 2026-05-04 |
| **H19** | `lib/utils/cors.ts` + endpoints `/api/sparkbot/*` | 2026-05-04 | CORS allowlist centralizado. Antes: `Access-Control-Allow-Origin: *` em 6 endpoints. Agora: regex allowlist (gohighlevel.com, leadconnectorhq.com, msgsndr.com, sparkleads.pro, próprio app, localhost dev). Inclui `Vary: Origin` pra cache-correctness. | audit security 2026-05-03 |
| **H20** | `embed/sparkbot/loader/route.ts:iframe` | 2026-05-04 | Iframe sandbox attribute: `allow-scripts allow-same-origin allow-forms allow-popups allow-modals`. Limita capacidades do iframe enquanto preserva UX (mic, fetch ao próprio domínio, links). | audit security 2026-05-03 |
| **H21** | `account-assistant/identity.ts:identifyRepByGhlUser` | 2026-05-04 | check-admin 500 em sub-account nova (Pedro reportou em v3xt9lZ3GhEGvyvl6sgN). Causa: GHL API não retornou phone, code caía em INSERT com `webonly:<ghlUserId>` placeholder, batendo unique violation com rep_identity criada em session anterior. Fix: lookup em camadas — (3a) por ghl_user_id em ANY rep, (3b) por phone OR placeholder. Append link se achou; senão cria. + DB cleanup: merge 84ab5b5b... no Pedro principal. | bug observado em prod 2026-05-04 |
| **H22** | `components/agents/account-assistant/setup-wizard.tsx` + `api/agents/sparkbot/onboarding-status/route.ts` | 2026-05-04 | Setup Wizard no AI Hub: card destaque com QR code WhatsApp + link wa.me + polling 5s do endpoint pra detectar primeira msg do rep. Auto-some quando ativa (`first_time=false`). Mostra aviso se admin não tem phone cadastrado no GHL (reason_no_phone). Lib `qrcode` adicionada. | conversa Pedro 2026-05-04 |
| **H23** | `account-assistant/tools/identity.ts:switchActiveLocation` + `block_calendar_slot` + `report_missed_capability` | 2026-05-04 | 3 tools novas: switch_active_location (rep multi-tenant troca sub-account ativa), block_calendar_slot (compromisso pessoal sem ser appointment com cliente), report_missed_capability (auto-registra "rep pediu X que não consigo" pro painel admin). | conversa Pedro 2026-05-04 |
| **H24** | `lib/account-assistant/tools/bulk-messages.ts` + 7 tools | 2026-05-04 | Tools de disparo em massa com drip mode (anti-WhatsApp ban): preview/schedule/list/get_progress/pause/resume/cancel. Filtro por tag, drip 90s±30s entre msgs, variation via Haiku, cap 100/dia/sub-account, quiet_hours obrigatório. Migration 00050. | conversa Pedro 2026-05-04 |
| **H25** | `app/admin/signals/*` + `lib/admin-signals/recorder.ts` + `middleware.ts` | 2026-05-04 | Painel /admin/signals (Basic Auth via env `ADMIN_PANEL_PASSWORD`) rastreando 4 tipos: failure/missed_capability/error/idea com fingerprint dedup (sha256 type+title). Hook em executeTool auto-registra erros. Tool `report_missed_capability` registra gaps de capacidade quando bot diz "não consigo". Migration 00051. | conversa Pedro 2026-05-04 |
| **H26** | `account-assistant/tools/calendar.ts` (createAppointment, updateAppointment) + helper `buildOverridePayload` | 2026-05-14 | 4 override flags em appointments. **3 destrutivas admin-only** via H17 `is_internal` (`ignoreFreeSlotValidation`/`ignoreDateRange`/`toNotify=false`) — gate em `buildOverridePayload`, audit signal `idea` com fingerprint estável (recorder dedupa → counter no painel). **`overrideLocationConfig` auto-ativado** pra qualquer rep quando passa `meeting_location_type`/`meeting_location` — conserta bug silencioso onde GHL descartava esses params. Schema NÃO expõe `overrideLocationConfig` standalone — sempre derivado dos params de meeting location. update_appointment ganha `meeting_location_type`/`meeting_location` (não existiam). `block_calendar_slot` fora do escopo (endpoint /block-slots não aceita flags). | conversa Pedro 2026-05-14 |
| **H27** | `account-assistant/filter-engine/*` (types, capabilities, cache, resolvers, compiler, executor, audit, interpolator, disclaimers) + `tools/filter-tools.ts` (4 tools novas) + migration 00063 (filter_executions) | 2026-05-15 | **Filter Engine universal** — fonte única de verdade pra filtrar contatos/opps. DSL JSON FEL (AND/OR/NOT aninhados, aliases auto stageName→stageId, customField por slug ou UUID, opportunity joins transparentes), capability matrix versionado (probe-friendly), paginação ilimitada com cap defensivo 5000 (env `FILTER_ENGINE_MAX_PAGES`), cache TTL 10min pra pipelines + customFields. 4 tools expostas ao LLM: `get_contacts_filtered`, `get_opportunities_filtered`, `count_filtered` (1-call otimizado), `describe_filter_capabilities`. Tools legacy (search_contacts, list_opportunities, list_birthdays_today) ficam como wrappers retrocompat — dateOfBirth client-side fallback automático via engine. Audit log em `filter_executions` table (debug + perf + abuse detect). | plano `_planning/filter-engine-and-bulk-v2.md` |
| **H28** | `account-assistant/tools/bulk-messages-v2.ts` + reuso helpers de bulk-messages.ts | 2026-05-15 | **Bulk Messages V2** sobre Filter Engine. 2 tools novas: `preview_bulk_message_v2` + `schedule_bulk_message_v2`. Multi-segment (N filters × N templates num job), dedup automático por contact_id, interleave opcional. Disclaimers tier: SEMPRE pergunta lista quente/fria; risk disclaimer dispara em >50 quentes ou >10 frios. Interpolação rica `{first_name}`, `{tags[0]}`, `{custom.slug}`, `{opportunity.stage_name}`. Snapshot do texto final por recipient (col `personalized_message`, audit). Migration 00063 adiciona `segment_label` + `personalized_message` em bulk_message_recipients. Bulk V1 (schedule_bulk_message) continua funcional como wrapper. Resolve caso Gustavo 2026-05-15 (mensagem diferente por stage). | plano `_planning/filter-engine-and-bulk-v2.md` seção 5 |
| **H29** | `account-assistant/conversational/{templates, turn-context, next-steps, smart-defaults}.ts` + wire em prompt-builder + processor | 2026-05-15 | **Conversational UX foundation**: 4 módulos novos. (1) 7 visual templates canônicos (LIST_RESULT, ACTION_PROPOSAL, SUCCESS_NEXT, ERROR_RETRY, MENU_OPTIONS, RECAP_HIGH_RISK, DIAGNOSTIC) — toda resposta cai num. (2) TurnContext layer registra entidades resolvidas (contact, opp, stage, etc) → próxima tool reusa sem re-perguntar (caso Gustavo "qual contato?"). (3) NEXT_STEP_MAP — após cada write tool, bot oferece 1 sugestão concreta. (4) Smart Defaults: timezone, location, assigned_to, channel, intervalo — bot NÃO pergunta, infere de rep.* e env. | plano `_planning/conversational-ux-master-plan.md` |
| **H30** | `account-assistant/conversational/{voice-detector, disambiguation, error-recovery, multi-action}.ts` + MAX_ITERATIONS 6→10 | 2026-05-15 | **Proactive UX**: (1) Adaptive voice (detect & mirror) — short/verbose/urgent/neutral, injetado dinamicamente. (2) Disambiguation by Elimination — ranqueia candidatos por score (last_activity, mentioned_in_turn, exact_match, tag relevante); auto-pick se top > 0.7 + gap > 0.2. (3) ERROR_RECOVERY_MAP — 12 padrões de erro com plano de recovery automático (slot ocupado → get_free_slots; duplicado → update; rate limit → retry; etc). (4) Multi-action chaining guidance + MAX_ITERATIONS bump pra suportar até 6 ações em chain no mesmo turn. | plano `_planning/conversational-ux-master-plan.md` |
| **H31** | `account-assistant/tools/recap.ts` + `tools/identity.ts:setVerbosityPreference` + `filter-engine/disclaimers.ts:formatDisclaimersChecklist` | 2026-05-15 | **Adaptive personalization**: (1) Recap mode tool — comando "recap"/"resumo" lista últimas writes da sessão em formato bullet. (2) Verbosity preference persistida em rep_profile.preferences.verbosity (brief/normal/detailed) — bot adapta TODAS respostas; rep fala "menos texto" e bot lembra. (3) Combined Disclaimers — quando há 2+ disclaimers, vira CHECKLIST único com "tudo ok" como aceite global (reduz 3 turns pra 1). Undo Last Write pulado (V2 opcional). | plano `_planning/conversational-ux-master-plan.md` |
| **H33** | `account-assistant/followup/` (10 módulos) + `tools/followup.ts` (8 tools) + `proactive/followup-runner.ts` + `proactive/followup-completion-notifier.ts` + migration 00067 + `webhook-handler.ts` hook + prompt-builder + dashboard tab | 2026-05-18 | **Follow-up Feature** — sequências inteligentes de mensagens agendadas pra contatos, criadas via chat (futuro: webhook). Domain dedicado `followup/` separado de bulk pra evolução independente. Schema próprio: `followup_sequences` (lifecycle + spam/approval/source modular), `followup_messages` (claim_token pro runner), `followup_events` (audit). Core service `createFollowupRequest(input, ghlClient?)` aceita source=chat/proactive_rule/webhook — modular pra triggers externos futuros. Pipeline: resolve contato (disambig) → carrega settings → resolve contexto (busca conversa GHL + Haiku summarize) → spam score híbrido (regras determinísticas score 0-100; medium chama Haiku refine com sentimento) → safety checks (channel/opt-out/duplicate) → generate sequence (Sonnet, 1-3 msgs, anti-spam estrito) → decideFlow(risk×approval_mode). Approval flow: adaptive (low=auto, medium=approval, high=block sugere internal_reminder), configurável via agent_configs.followup_approval_mode. Pause-on-reply imediato via hook no webhook-handler.ts:processInbound (não espera próximo runner tick). Runner separado (`followup-runner.ts`, cron */1min) faz claim atomico, recheck sequence.status + inbound desde started_at, send via /conversations/messages com fallback WhatsApp→SMS, marca completed quando todas msgs terminal. Notifier proativo: completed (✅ X/Y enviados) ou paused-by-reply (📩 contato respondeu). 8 tools chat: create_followup_request (entry, retorna needs_user_decision se faltar context), approve/cancel/pause/resume/edit, list_my_followups, get_followup_progress. Dashboard tab Follow-ups: funnel 30d (total/aprovados/respondidos/bloqueados), risk breakdown, sequences ativas com progress bar, recent completed, events 7d. Smoke test 16/16 PASS. | plano `_planning/followup-feature.md` |
| **H32.6** | múltiplos arquivos do H32 | 2026-05-16 | **Bulk Platform code review fixes**. Code review agent identificou 5 críticos + 10 high + 15 med + 10 low. Fixados: (C1) `bulk-runner-rep-notifier` merge JSONB profile completo pra não sobreescrever preferences/aliases. (C2) `countRecipientsLast24h` + `countRecipientsLast7Days` excluem `failed` (não só cancelled/skipped) — recipients que nunca vão sair não inflam cap. (C3) `spread_days` cap math: soma remaining REAL de cada dia (não assume cap cheio em dias 2..N) — bug pseudo-Gustavo em multi-day fixado. (C4) Runner paused→pending revert loop removido (priority queue F4.1 já filtra paused upstream — branch nunca deveria bater). (C5) Rollback usa `cancelled_reason` + `status='failed'` (não coluna inexistente `error`). (H1) Timezone DST-correct via `Intl.DateTimeFormat` em `toEtDayString` helper — substitui offset hardcoded -4h em 4 lugares (count, getEffectiveDailyCap, bulk-management cap_status, request_cap_override). (H2) Jaccard tokens ≥2 chars (não >2) + normaliza accents + remove placeholders {} — anti-dup PT-BR funciona. (H3) `bulk_reschedule_job` batch paralelo (chunks 50) em vez de O(N) serial. (H4) `bulk_dashboard` 2N→2 queries via batch aggregate. (H5) Anti-dup checa CADA segment (não só primeiro) + dedup por job_id. (H7) Heartbeat tracking explícito (`heartbeatOk`). (H8) `effectiveSendDate` usa `adjustedStartAt` pré-computado (quiet-hours consistent). (H9) Remove "(em breve)" das msgs de trim — F2.7 já implementou. (M2) V1 pause/resume/cancel + reschedule/edit checam `location_id` — RBAC multi-tenant. (M5) `delivery_strategy.interval_seconds` tem prioridade sobre top-level. (M7+M10) Cap math docs clarificados. (M9) Processor silence-gap query filtra `active_location_id`. (M12) Reschedule/edit valida hex prefix antes ILIKE — anti-SQL-injection. (M13) Trim reverse documentado + prompt orienta segments por prioridade DESC. (M14) Default 100→300 commented com nota operacional. (M15) `list_bulk_jobs` + `bulk_dashboard` autoRegister robusto. (L1) `void` cleanup (3 imports unused removidos). (L6) COMMENT ON colunas via SQL prod. (L10) Texto rep notifier suavizado. Smoke 49/49 re-validated. | code review independente 2026-05-16 |
| **H32.5** | `account-assistant/conversational/turn-context.ts` (loop detection + bulk state) + `conversational/silence-recovery.ts` (novo módulo) + `proactive/bulk-runner-rep-notifier.ts` (novo) + `processor.ts` (silence detection query) + V1 deprecation + prompt-builder | 2026-05-16 | **Bulk Platform Fase 5 — Conversational extensions + rep alerts**. (4.1) Loop detection: `repeated_questions: Record<string,number>` em TurnContextState; helpers `recordQuestion(cat)` + `questionCount(cat)`; render bloco "⚠️ ALERTAS DE LOOP" quando ≥2; prompt instrui bot a NÃO repetir e oferecer alternativas (assumir escolha segura, reformular diferente, reconhecer confusão). Caso Gustavo: 2x tela cap idêntica em 90s evitada. (4.2) Bulk-specific turn-context: `bulk_session_state` registra cap_choice/warm_status/delivery_choice_id/scheduled_at_chosen/accepted_disclaimers/last_preview_job_id/last_preview_total_contacts; `autoRegisterFromToolResult` extrai automaticamente de preview_bulk_message_v2 + schedule_bulk_message_v2; próximas tools reusam sem re-perguntar; helper `recordBulkChoice(state, key, value)`. (4.3) Silence recovery: novo módulo `silence-recovery.ts` com `detectSilenceGap(messages, 30min)` + `renderSilenceRecoveryForPrompt`; processor faz query rápida nas últimas 6 sparkbot_messages com created_at; se gap >30min E última do bot, injeta bloco "# ⏰ SILENCE GAP DETECTADO" no system prompt orientando bot a reconhecer e oferecer retomar de onde parou. Caso Gustavo 5h15min silêncio fixado. (V1 deprecation) `preview_bulk_message` + `schedule_bulk_message` descriptions marcadas ⚠️ DEPRECATED orientando LLM a usar V2; tools ficam funcionais como compat mas LLM evita. (Rep alerts) `bulk-runner-health-check.ts` agora além de signal admin também invoca `bulk-runner-rep-notifier.ts` que envia WhatsApp pros reps com jobs travados; dedup 30min via `rep.profile.bulk_stall_notified_at` evita spam; mensagem oferece comandos rápidos ("meus disparos" / "pausa todos"). | plano `_planning/bulk-management-platform.md` Section 4 + open questions |
| **H32.4** | `account-assistant/tools/bulk-messages.ts` (findSimilarActiveJobs Jaccard) + `proactive/bulk-message-runner.ts` (priority queue + cooldown record) + `bulk-messages-v2.ts` (priority/label/similar_jobs no preview/schedule) + migration 00066 + prompt-builder | 2026-05-16 | **Bulk Management Platform Fases 3+4** — smart cap + priority/labels. (F3.1) Default cap diário 100 → 300 em `DEFAULT_DAILY_BULK_CAP`; sub-accounts com cap explicit no DB não afetadas. (F3.2) Per-contact cooldown WARN-ONLY em `bulk_contact_cooldown` table; runner faz UPSERT após cada send, preview consulta últimos 24h e expõe `cooldown_warnings[]` + `cooldown_warning_summary` (NÃO bloqueia, Pedro escolheu). (F3.4) Weekly cap secundário opcional em `agent_configs.weekly_bulk_message_cap`; schedule_v2 BLOQUEIA hard se exceder rolling 7d (sem override — proteção contra abuso). Helpers `getWeeklyCap` + `countRecipientsLast7Days`. (F4.1) Priority queue: `bulk_message_jobs.priority` 1-100 default 50; runner pega top MAX_PER_TICK×4 candidatos por scheduled_at ASC, ordena client-side por priority DESC + scheduled_at ASC, dá update atomico em MAX_PER_TICK. (F4.2) Job labels: `bulk_message_jobs.label` opcional human-readable; dashboard mostra label se existir senão segments+template; preview/schedule aceita arg `label`. (F4.3) Anti-duplicação via Jaccard similarity sobre tokens >2 chars do template (threshold 0.7); preview retorna `similar_active_jobs[]` + `similar_jobs_warning`; bot pergunta merge vs novo antes de schedule. Runner registra cooldown async/silent após cada send bem-sucedido. Dashboard exibe badge 🔥urgente (priority>=70) ou 🐌background (priority<=30). | plano `_planning/bulk-management-platform.md` Fases 3+4 |
| **H32.2** | `account-assistant/tools/bulk-management.ts` (7 tools) + `bulk-messages.ts:getEffectiveDailyCap` + migration 00065 + prompt-builder | 2026-05-16 | **Bulk Management Platform Fase 2** — 7 tools novas pra rep gerenciar múltiplos disparos via WhatsApp sem precisar abrir admin panel: (1) `bulk_dashboard` (safe) — visão consolidada com active_jobs/recent_completed/alerts/cap_status/runner_health + dashboard_summary formatado pronto; (2) `bulk_pause_all` (medium) — pausa todos running em 1 call; (3) `bulk_resume_all` (medium) — retoma todos paused; (4) `bulk_cancel_all` (high) — cancela todos ativos com audit reason; (5) `bulk_reschedule_job` (medium) — move job pra outra data, recalcula scheduled_at de todos pending mantendo espaçamento; (6) `bulk_edit_pending_job` (high) — edita template/variation/interval/jitter de job ativo, re-interpola personalized_message dos pending; (7) `bulk_request_cap_override` (high) — eleva cap diário com audit em `bulk_cap_overrides` table, HARD CEILING 3x do base (default 100 → max 300). `getEffectiveDailyCap` soma overrides ativos no cap base por dia; V2 schedule usa automaticamente. Migration 00065: bulk_cap_overrides table + cols paused_at/cancelled_reason/cap_override_id em bulk_message_jobs. Suporta job_id prefix 8 chars em todas as ops individuais. | plano `_planning/bulk-management-platform.md` Fase 2 |
| **H32** | `account-assistant/tools/bulk-messages.ts:countRecipientsLast24h` + `bulk-messages-v2.ts` (cap + trim msg + coexist warning) + `filter-engine/disclaimers.ts:formatDisclaimersChecklist` ativado + `proactive/bulk-message-runner.ts` heartbeat + `proactive/bulk-runner-health-check.ts` + migration 00064 + prompt-builder | 2026-05-16 | **Bulk Management Platform Fase 1** — 6 fixes pós caso Gustavo (14 turns em loop por bug de cap futuro). (F1.1) `countRecipientsLast24h(loc, windowDate?)` conta por DIA DE ENVIO (scheduled_at), não por created_at — antes 63 recipients agendados pra terça 19/05 contavam contra cap do dia 16/05, truncando M3 de 6 → 2 silenciosamente. Validado SQL: dia 16 antes=63, depois=0. (F1.2) V2 usa `formatDisclaimersChecklist` quando 2+ disclaimers (1 turn `tudo ok` em vez de N). Bug template literal `${disclaimers.length}` fixado. (F1.3) Prompt-builder removeu pergunta "é quente?" SEPARADA — inferir do contexto (rep com interação prévia = warm) ou cair no checklist único. (F1.4) `cap_trim_info` + `cap_trim_message` no response do schedule — bot não inventa mais "pode ser stage no CRM" quando trim é por cap. (F1.5) `bulk_runner_health` singleton com `last_tick_at` + `consecutive_errors` + `bulk_runner_stale_v` view; runner faz heartbeat upfront e atualiza counters; `bulk-runner-health-check.ts` cria signal admin quando >5min stale OU jobs running com pending overdue há >10min sem sent_count mover há 30min. (F1.6) Coexistência NÃO bloqueia mais schedule — só retorna `coexistence_warning` informativo no response (Pedro: "fazer vários ao mesmo tempo"). | plano `_planning/bulk-management-platform.md` |

## Críticos novos da ULTRA-REVIEW 2026-05-05 (`C5-C13`)

| Code | File:line | Data | Sumário | Ref |
|------|-----------|------|---------|-----|
| **C5** | `account-assistant/terms.ts:parseTermsResponse` | 2026-05-05 | Falso positivo "não tá ok" → ACCEPT por causa do `.includes(' ' + k)` em ACCEPT_KEYWORDS. Fix: NEGATION_PATTERN check ANTES de ACCEPT, normaliza acentos via NFD. LGPD risk corrigido. | ULTRA-REVIEW Track 1 C2 |
| **C6** | `account-assistant/processor.ts:99` + `identity.ts:rejectTerms` + migration 00052 | 2026-05-05 | Rejeição de termos não persistia → loop infinito reenviando termos. Adicionado `terms_rejected_at` na rep_identities + gate em processor que silencia bot daqui em diante. | ULTRA-REVIEW Track 1 C1 |
| **C7** | `account-assistant/identity.ts:identifyRep` | 2026-05-05 | Race rep_identity 23505 não capturado. 2 webhooks Stevo+GHL <100ms ambos passavam dedup, ambos tentavam INSERT, segundo falhava com error code 23505 — bot retornava null → "rep não cadastrado". Fix: capture 23505 + re-fetch row criada pelo competidor. | ULTRA-REVIEW Track 1 C3 |
| **C8** | `account-assistant/tools/reminders.ts:cancel_reminder` | 2026-05-05 | validateGhlId rejeitava UUIDs (regex `/^[A-Za-z0-9]+$/` não aceita hífens). 100% dos cancels falhavam em prod. Substituído por UUID v4 regex validation. | ULTRA-REVIEW Track 4 CRIT-1 |
| **C9** | `account-assistant/tools/identity.ts:switch_active_location` | 2026-05-05 | switch_active_location não recriava ctx.ghlClient após mutar locationId. Próximas tools no mesmo turn usavam token da location ANTIGA → cross-tenant leak risk (body.locationId NEW + Auth OLD). Agora reinstancia via dynamic import. | ULTRA-REVIEW Track 4 CRIT-2 |
| **C10** | `account-assistant/tools/calendar.ts:block_calendar_slot` | 2026-05-05 | Body inválido per spec Spark Leads: enviava `calendarId + assignedUserId` juntos (spec diz "either, not both") + campo `notes` que não existe. Tool 100% quebrada desde criação. Fix: só assignedUserId + sem notes. | ULTRA-REVIEW Track 4 CRIT-3 |
| **C11** | DB UPDATE `assistant_proactive_rules` | 2026-05-05 | 3 reactive rules `enabled=true` mas STUBADAS no código (Lead esfriando, Tarefa atrasada, Task vencendo). UI enganava rep mostrando ON. Disabled em prod até implementação. Apenas post_meeting é real. | ULTRA-REVIEW Track 6 C1 |
| **C12** | migration 00053_cron_guards_advisory_lock | 2026-05-05 | Cron pg_cron rodava 30s/30s independente de haver trabalho (pós-00041 removeu WHERE EXISTS guard) = 2880 calls/dia desnecessárias + sem advisory lock = double-execution risk. Recriado com pg_try_advisory_xact_lock(8675309) + WHERE EXISTS triplo (scheduled_tasks + proactive_rules + bulk_message_recipients). **⚠️ Correção 2026-06-10 (ver NB-7):** a parte "advisory lock previne double-execution" está ERRADA — o lock é xact-scoped e o `net.http_post` (pg_net) é assíncrono (solta no commit do tick, ANTES do POST sair), então NÃO serializa os lambdas/execuções do endpoint; só impede co-enfileiramento de 2 transações de disparo no mesmo instante. Anti double-execution real = claims atômicos CAS por linha nos runners (reminder `pending→running`, bulk `pending→sending` via `claim_bulk_recipients` FOR UPDATE SKIP LOCKED). O WHERE EXISTS triplo (anti calls vazias) segue válido; o lock fica só como guard barato. | ULTRA-REVIEW Track 6 H3 + Track 12 C1 |
| **C13** | migration 00056_usage_records_schema_drift_recovery | 2026-05-05 | Schema drift catastrófico: charge.ts inseria 8 colunas (cached_tokens, audio_seconds, image_count, claim_token, claimed_at, charged_at, cache_creation_tokens, audio_model) que NÃO existiam na tabela DB real (criada via SETUP.sql pré-00040). Supabase silently dropava → Whisper/Vision/cache billing quebrado em prod. ALTER TABLE retroativa. | ULTRA-REVIEW re-validation 2026-05-05 |
| **C14** | `account-assistant/tools/calendar.ts:calendarHasOpenHoursAt` | 2026-05-05 | Função usava `getUTCDay/getUTCHours` mas openHours/daysOfTheWeek estão em LOCAL time. Pra rep em EDT (UTC-4), slot 14:00 EDT = 18:00 UTC → função comparava 18 com closeHour=18 → fora de BH → INTERSECT-conservador virava no-op silencioso. Caso Marcos podia reaparecer sem detecção. Fix: Intl.DateTimeFormat com timeZone do rep pra extrair local weekday/hour/minute corretos. 8 unit tests passam. | calendar bug-proof re-review 2026-05-05 |
| **C15** | `account-assistant/tools/calendar.ts:listMyFreeSlots` | 2026-05-05 | Tool USER-CENTRIC (`list_my_free_slots`) criada separada de CALENDAR-CENTRIC (`get_free_slots`). Bug histórico: bot misturava semânticas, calculava livre via `list_appointments` + reasoning manual → perdia Google Calendar blocks (cliente Marcos perdeu credibilidade). Fix arquitetural: 2 tools dedicadas — UNION de /free-slots dos rep's calendars + subtract events cross-calendar (filter client-side por assignedUserId, não passa userId que filtra por createdBy) + INTERSECT-conservador best-effort pra detectar Google blocks via gap entre calendars com BH coverage. | bug observado em prod Marcos 2026-05-05 |
| **C16** | `account-assistant/tools/calendar.ts:listMyFreeSlots` + types `ToolResult` | 2026-05-05 | Quando TODOS os event lookups falhavam, tool retornava status:ok com slots — bot apresentava como livres sem ter detectado conflicts. Adicionado novo status `degraded` no `ToolResult` union + warning crítico no payload + prompt instrui LLM a SEMPRE pedir confirmação verbal antes de marcar appointment a partir de slot degraded. computeWindowInTz também ganhou fallback "America/New_York" pra tz inválido (RangeError defense). | calendar bug-proof re-review 2026-05-05 |
| **C17** | `account-assistant/tools/calendar.ts:startOfDayInTz` | 2026-05-05 | BUG GRAVE descoberto via test direto contra GHL real (script `scripts/test-marcos-free-slots.ts`): algoritmo antigo `utcMidnight - h*60min` funcionava só pra timezones com offset POSITIVO (Asia/Europe/Pacific). Pra negative offsets (Americas), calculava midnight do dia ANTERIOR no tz. ROOT CAUSE provável do caso Marcos original — bot retornava window do dia errado pra TODOS reps US. Fix: calcula offset real do tz comparando "what tz sees" vs UTC com Date.UTC(), funciona pra qualquer offset. | test direto Marcos 2026-05-05 |
| **C18** | `account-assistant/tools/calendar.ts:listMyFreeSlots INTERSECT-conservador` | 2026-05-05 | Bug observado via screenshot agenda visual Marcos: heurística era too eager — qualquer 1 calendar missing virava "suspect block", removendo slots GENUINAMENTE LIVRES. Caso real: Marcos tem 7 calendars, mas só 6 usam Work Hours schedule + 1 (Calendário-Agência) é admin-only (nunca retorna slot público). INTERSECT antigo via "Agência missing + BH OK" e removia slots → 0 livres em dia com 4h livres. Fix: (a) silent calendars (zero slots em todo range) filtrados do baseline, (b) threshold 50%+ missing (não 1+), (c) min 3 calendars-com-BH pra ter sample size confiável. | screenshot validation 2026-05-05 |
| **C19** | `lib/ghl/auth.ts:getLocationToken` | 2026-05-05 | Token mutex em getLocationToken — antes, list_my_free_slots disparava 37 requests paralelas que se token expirado batiam todas em /oauth/locationToken simultaneamente (rate limit risk + token invalidation race). Fix: inFlightTokenRefresh Map coalesce concurrent refreshes — só 1 fetch real, restantes aguardam mesma promise. Afeta TODO o sistema (não só calendar). | bug-proof audit 2026-05-05 HIGH-1 |
| **C20** | `account-assistant/tools/calendar.ts:listMyFreeSlots events handling` | 2026-05-05 | Distinguir eventsFailed (transient: 5xx/network) de eventsMissingField (200 OK sem events array — silent GHL corruption) de eventsNotFound (404 calendar deletado). Antes, payload corrompido contava como sucesso "0 events" e bot apresentava livre com falsa confiança; 404 inflava warning_partial desnecessariamente. | bug-proof audit 2026-05-05 CRIT-2 + CRIT-5 |
| **C21** | `account-assistant/tools/calendar.ts:listMyFreeSlots conflict check` | 2026-05-05 | Per-source duration tracking. Antes Math.max usava maior duration → calendar A 30min livre 14h era bloqueado por conflict 14:30 porque calendar B 60min conflitava. Agora slot só removido se TODAS as sources conflitam (mais permissivo, evita esconder livre legítimo). + dedup conflicts por event.id (mesmo event 2x se rep é assignee em 2 calendars). | bug-proof audit 2026-05-05 HIGH-4 + MED-3 |
| **C22** | `account-assistant/tools/calendar.ts:calendarHasOpenHoursAt` | 2026-05-05 | Suporte a overnight calendars (open 22:00 → close 02:00). Antes closeMin < openMin → no-op silencioso, calendars noturnos tratados como NUNCA abertos. Agora detecta wrap-around: `slotMin >= openMin OR slotMin < closeMin`. | bug-proof audit 2026-05-05 MED-1 |
| **C23** | `account-assistant/tools/calendar.ts` (defesa em profundidade) | 2026-05-05 | Múltiplos hardenings: (a) encodeURIComponent em URL paths com IDs (defesa contra path injection), (b) slotDuration validation (rejeita 0/null/negative), (c) endTime <= startTime → ignora event corrupto (antes virava "ocupado dia todo"), (d) cancel statuses inverter pra positive list (antifragil contra GHL adicionar status novo), (e) parsing defensivo slots (string corrupta vira [], date-shape validation pra keys), (f) listAppointments tz-aware (antes setHours UTC servidor). | bug-proof audit 2026-05-05 MED-2/4/5, HIGH-6, LOW-1/3/4 |
| **C24** | `lib/ai/prompt-builder.ts:buildResponseJsonSchema (book_appointment)` | 2026-05-05 | OpenAI strict mode (response_format=json_schema) exige TODAS keys de `properties` em `required`. Variant `book_appointment` tinha `title` em properties mas faltava em required → 400 "Missing 'title'". Sales/recruitment test mode bot persistia user msgs mas NUNCA respondia. Fix: required inclui title + title vira `["string", "null"]` nullable. Outras 5 variants já estavam corretas. | bug observado em prod test mode 2026-05-05 |
| **C25** | `lib/ai/action-executor.ts:executeActions` | 2026-05-05 | testMode polui conversation_state real. Antes, /test com execActions=true + contact_id real corrompia state de prod (last_ai_response_at, message_count, status). Bot real ficava com follow-ups cancelados, summary gerado antecipadamente. Fix: skip updateConversationState quando ctx.testMode=true. | deep review 2026-05-05 CRIT-1 |
| **C26** | `lib/queue/processor.ts:processGroup` + `lib/ai/openai-client.ts` | 2026-05-05 | Empty content em user msgs causava Claude 400. (a) processor.ts: aggregatedBody trim vazio → fallback "[contato enviou conteúdo não processável]". (b) openai-client.ts: turns no histórico com content="" → substitui por "[mensagem vazia]" antes do push (em ambos paths OpenAI e Claude). Pre-existing rule "nunca persistir vazio" só protegia INSERT, não a leitura. | deep review 2026-05-05 CRIT-2 + CRIT-3 |
| **C27** | `lib/ai/prompt-builder.ts:buildSystemPrompt (override path)` | 2026-05-05 | system_prompt_override SHORT-CIRCUITAVA, pulando recruitment guard/KB/identity/booking. Recruitment com override virava vendedor silenciosamente, agente com KB grande perdia toda KB. Fix: override agora COMPÕE com sections críticas (mantém anti-vendas + KB + identity + booking format) — admin customiza voz mas não contorna regras de produto. | deep review 2026-05-05 HIGH-7 |
| **C28** | `lib/ghl/operations.ts:isBookingConflictError` | 2026-05-05 | Keyword "calendar"/"calendario" era genérica demais — erro de CONFIG ("Calendario nao configurado") batia como conflict. Action-executor mandava "tenta outro horário" ao lead em loop infinito. Fix: keywords específicas de CONFLICT (slot/conflict/busy/422) + BOOKING_CONFIG_KEYWORDS exclude list (não configurado/missing). | deep review 2026-05-05 HIGH-11 |
| **C29** | `app/api/webhooks/inbound-message/route.ts:nextWorkingHourStart` | 2026-05-05 | Working hours dropava msgs SILENCIOSAMENTE. Lead fora-hora ficava sem resposta jamais. Fix: novo helper calcula próximo expediente (ate 7 dias), enfileira com `process_after`. Cron pega quando volta. | deep review 2026-05-05 HIGH-13 |
| **C30** | `lib/ai/openai-client.ts:isClaude` | 2026-05-05 | Hardcoded list `CLAUDE_MODELS = ["claude-sonnet-4-6", "claude-haiku-4-5", "claude-opus-4-6"]`. Quando Claude lançasse versão 4-7+, caía em OpenAI client → erro modelo desconhecido. Fix: prefix-based `model.startsWith("claude-")`. | deep review 2026-05-05 MED-7 |

## Priority (`P<n>`)

| Code | File:line | Data | Sumário | Ref |
|------|-----------|------|---------|-----|
| **P0** | `lib/billing/charge.ts:26` | 2026-04-28 | `usage_records` referenciada no código mas tabela não existia em prod. Migration 00040 criou. Drift recovery: `chargeUnbilledRecords()` cobra retroativamente. | `_planning/_review-2026-04-28/code-review/billing.md` |
| **P0** (audio) | `lib/ai/history-compressor.ts:39` | 2026-04-28 | Cobrança opcional de audio_seconds via `audioMetaSink` — antes Whisper rodava free. | review 04-28 |
| **P1** | `lib/billing/charge.ts:198` | 2026-04-28 | Claim atômico via `UPDATE ... RETURNING` pra evitar double-charge em retries concorrentes. | review 04-28 |

## New Bugs from validation (`NB-<n>`)

| Code | File:line | Data | Sumário |
|------|-----------|------|---------|
| **NB-1** | (validation 2026-05-02) | 2026-05-02 | Cleanup duplo de `releaseInFlight` em path de dedup SELECT — cosmético, `Map.delete` é safe no-op. |
| **NB-2** | webhook-handler.ts mutex | 2026-05-02 | `inFlightMessages` Map é per-lambda. Documentado nos comments — race cross-lambda fica pra UNIQUE constraint pegar. |
| **NB-3** | webhook-handler.ts:30-39 | 2026-05-02 | GC entries expiradas só roda em entry de `tryClaimInFlight`. Bound: traffic × 60s. Acceptable. |
| **NB-6** | `webhook-handler.ts:410` | 2026-05-02 | Sticky tabular cache extendido pra `kind === "audio"` — rep manda CSV → confirma via voice memo. Antes só `kind === "text"`. |
| **NB-7** | `proactive/runner-health.ts:79` + migration `00100_runner_health_atomic_increment.sql` | 2026-06-10 | `trackRunner` incrementava `consecutive_errors` via read-modify-write (SELECT → +1 em JS → UPSERT) com comment afirmando "single-worker (cron tick)" — FALSO. O pg_cron `sparkbot-proactive` (00053) dispara a cada 30s, `maxDuration=60`, e o `pg_try_advisory_xact_lock(8675309)` é _xact_-scoped (solta no commit do tick que só enfileira o `net.http_post`) → NÃO serializa os lambdas Vercel. Ticks sobrepostos liam o mesmo counter e gravam o mesmo +1 = lost update, subcontando a streak e atrasando o admin_signal de `>= 3`. Fix: RPC `increment_runner_error` (INSERT .. ON CONFLICT DO UPDATE SET `consecutive_errors = runner_health.consecutive_errors + 1` RETURNING); TS usa o valor pós-incremento pro check. Severity baixa (counter interno, self-healing + path redundante `checkBulkRunnerStaleAndAlert`). |
| **NB-8** | `lib/queue/summary-note-generator.ts:256` (success-path save) | 2026-06-10 | Mesma classe do NB-7 (lost-update por write sem guard). O success-path do summary-note gravava `summary_note_id = noteId` filtrando só por `agent_id + contact_id`, enquanto o reset do catch-path logo abaixo já guardava com `.eq("summary_note_id","generating")` — assimetria = risco latente: um segment-reset concorrente (`action-executor.updateConversationState`: `summary_note_id→null` + `segment_number+1` + `ai_paused_at=null`) poderia ser clobberado de volta pro noteId antigo. **NÃO é bug vivo** — o lock atômico (`null→"generating"`, L47) + o reset só disparar em noteId REAL (nunca `"generating"`) impedem o cenário hoje; é defesa-em-profundidade. Fix: success-path ganha `.eq("summary_note_id","generating")` + `.select().maybeSingle()` — grava o noteId só se ESTE processo ainda detém o lock. Se casar 0 linhas (reset concorrente já moveu a row), a nota no GHL JÁ foi criada (side-effect preservado) → só loga (`state_written:false` no execution_log) e NÃO reescreve. Espelha o guard do catch-path. | hardening 2026-06-10 |
| **NB-9** | `account-assistant/filter-engine/resolvers.ts` (`resolveCustomFieldRef`, ex-`looksLikeGhlUuid`) + teste `scripts/test-resolver-uuid.ts` | 2026-06-10 | **Heurístico de shape classificava slug como id → busca silenciosa de zero em prod.** `looksLikeGhlUuid` (`/^[A-Za-z0-9]{18,}$/`) decidia "isto já é id?" ANTES de tentar o slug, chamado nos refs de `customField.`/`opportunity.customField.`. Qualquer fieldKey/slug SEM separador com 18+ chars (`averageannualpremiumrange`, `clientpolicyanniversary`, `policyanniversarydate`) batia o regex → resolver pulado → slug cru mandado downstream. Efeito silencioso (ZERO erro): contact CF + op server-side (`eq`) virava `customFieldId=<slug>` no contacts_search_v2 (GHL devolve 0 sem erro); opportunity CF / ops client-side caíam em `extractFieldValue` (`cfs.find(c => c.id === ref)` = undefined em toda linha). GHL gera fieldKey com `_` (espaços→underscore), então multi-palavra escapava; campos de 1 palavra longa ou key manual/importada caíam. Fix: a lista de CFs é a fonte da verdade (re-fetch em cache stale) — resolve por slug/fieldKey/name PRIMEIRO, só trata como id literal se bater num `cf.id` real, senão `ALIAS_NOT_FOUND` (erro útil, não zero silencioso). Elimina falso-positivo (slug≥18→id) E falso-negativo (id curto→ALIAS_NOT_FOUND espúrio). Zero heurístico de shape. Teste 7/7. | review 2026-06-10 |
| **NB-10** | `supabase/migrations/00106_dispatch_claim_cooldown0_race.sql` (reescreve `try_claim_dispatch_slot` da 00033) + teste `scripts/test-dispatch-claim-race.ts` | 2026-06-10 | **Claim atômico de dispatch proativo NÃO serializava `cooldown_minutes <= 0` → double dispatch latente.** A branch `IF p_cooldown_minutes <= 0` fazia `INSERT .. ON CONFLICT DO UPDATE` SEM `WHERE` e SEMPRE `RETURNING id` — duas chamadas concorrentes de `dispatchRule` pro MESMO (rep_id, rule_id, target_id) ganhavam AMBAS um id não-nulo e seguiam pra LLM+envio. O `WHERE last_fired_at < cutoff` que serializa só cobria `cooldown > 0`. Regras afetadas (`account-assistant/proactive/system-rules.ts`): "Deal fechado"/`deal_won` e "Novo lead atribuído"/`contact_assigned_to_rep` (ambas `cooldown_minutes: 0`). **LATENTE, não vivo:** nenhum path de prod wireia esses eventos pro `dispatchRule` — `event-router.ts` manda `OPPORTUNITY*`/`CONTACT*` pra "Etapa 4 (só log)", o cron `processReactivePolling` só implementa `post_meeting`, e o caminho de teste (`simulate-rule`) usa `forceFire=true` (branch upsert separado no dispatcher, não passa pela função). Vira REAL quando deal_won/novo-lead forem ligados ao dispatchRule (TODO da Etapa 4): msg proativa duplicada pro rep + double-charge de tokens LLM, blast radius 1 (rep,rule,target), rep-facing, sem vazamento de dado. Mesma classe do NB-7/NB-8 (escrita concorrente sem guard) — mas aqui o guard simplesmente NÃO existia pra cooldown 0. Fix: unifica as duas branches; `cooldown <= 0` passa a usar um PISO anti-race de 2s (`cutoff = now() - 2s` em vez de "sempre permite") — a 2ª chamada vê o `last_fired_at` recém-gravado da 1ª, o WHERE falha, RETURNING vazio → NULL. Targets DISTINTOS furam o piso (cada deal comemorado 1x); não é cooldown de produto, só anti-duplicata do MESMO target em <2s. Prova (PG14 local, tx sobrepostas): OLD = 2 winners (bug confirmado), FIXED = 1 winner; 6/6 cenários (core paralelo, piso 2s, por-target, regressão cooldown>0, piso expira). Bônus: função trazida à postura SECURITY DEFINER + search_path fixo + EXECUTE travado em service_role (00088/00100). | review 2026-06-10 |
| **NB-11** | `supabase/migrations/00105_claim_bulk_recipients_priority.sql` (RPC `claim_bulk_recipients`) + `proactive/bulk-message-runner.ts` (`claimBulkRecipients`, `selectClaimBatch`, fallback legado) + teste `scripts/test-bulk-priority-claim.ts` | 2026-06-10 | **Priority queue (F4.1) do bulk-runner NÃO furava fila sob backlog — o único cenário onde priority importa.** O claim atômico de `fireBulkRecipients` fazia 2 passos: (1) SELECT recipients pending vencidos JOIN jobs `ORDER BY scheduled_at ASC LIMIT MAX_PER_TICK*4 (=20)` (buffer); (2) reordena CLIENT-SIDE por `jobs.priority DESC` e fatia 5. A ordenação por priority só acontecia no passo 2, sobre o buffer de 20 já escolhido por scheduled_at. Quando ≥20 recipients de jobs de prioridade BAIXA já estão vencidos com timestamps antigos (backlog drenando após quiet_hours/working_hours ou gap de deploy), o buffer de 20 é 100% consumido por eles e um job de prioridade ALTA com scheduled_at MAIS NOVO nunca entra na janela → o "fura fila" (priority 70-90, documentado pro LLM em prompt-builder/bulk-messages-v2) falha justo quando importa. **P3:** self-healing (drena ~10/min: MAX_PER_TICK=5 a cada 30s), zero perda de dado, mas derrota silenciosamente uma feature paga. **Por que não `.order(referencedTable)` no PostgREST:** ordenar o top-level por coluna de um embed to-one (M2O) é NO-OP — `.order(col,{referencedTable:'bulk_message_jobs'})` só ordena DENTRO de um embed to-many (array). Verificado empiricamente contra o PostgREST de prod (probe read-only): ordenar por `created_at` do embed em ASC vs DESC devolvia a MESMA ordem top-level (cláusula ignorada). Logo a ordenação TEM que ser SQL. Fix: RPC `claim_bulk_recipients(p_limit, p_claim_token)` (00105) faz o claim num passo — `ORDER BY j.priority DESC, r.scheduled_at ASC` ANTES do LIMIT + `FOR UPDATE SKIP LOCKED` (fecha de quebra a race select→update que o passo-2 só mitigava com `.eq('status','pending')`); carimba claim_token+claimed_at (H37); `RETURNS SETOF bulk_message_recipients`. O runner mantém o sort client-side como tiebreaker (UPDATE..RETURNING não garante ordem) e cai num caminho LEGADO (buffer maior + sort) se a RPC ainda não existir (gap de deploy: código no Vercel + migration não aplicada via MCP → detecta PGRST202). Postura SECURITY DEFINER + search_path + EXECUTE service_role (00088/00100). Reusa o index `idx_bulk_recipients_due`. Teste 7/7 (backlog 22 baixos + 5 altos → altos reivindicados primeiro; RPC + fallback + propagação de erro). | review 2026-06-10 |

## Sem código mas relevante (decisões de produto)

| Tema | Data | Decisão |
|------|------|---------|
| **Multi-hub Sparkbot** | 2026-05-02 | Aceita qualquer location com agent ativo `account_assistant`. Antes era single-hub via `ASSISTANT_HUB_LOCATION_ID`. |
| **GHL multi-provider dedup** | 2026-05-03 | Stevo (Evolution) + WhatsApp Business API ambos plugados. Cada msg física gera 2 webhooks com `messageId` diferentes. Stack de 7 camadas de dedup. |
| **OpenAI quota alert** | 2026-05-03 | Quando Whisper retorna 429, msg específica ao rep + log destacado. Sem isso, falha aparecia como "Não consigo processar áudio". |
| **Claude rejeita user msg vazio** | 2026-05-03 | Filter `content=""` antes de mandar histórico ao LLM + nunca persistir vazio. Cleanup retroativo de 4 rows. |
| **Outbound channel routing** | 2026-05-02 | `ASSISTANT_OUTBOUND_CHANNEL` env (`SMS` default agora; `auto` futuro com window 24h check). |
| **xlsx CVE — risco aceito** | 2026-05-27 | `xlsx@0.18.5` tem CVEs high sem fix no npm (proto pollution GHSA-4r6h-8v6p-xvw6 + ReDoS GHSA-5pgg-2g8v-p4x9; lib abandonada no npm). MANTIDO: parse só roda em upload de rep/admin **autenticado** (anexo SparkBot / base de conhecimento) com limite de tamanho — não exposto a lead externo → exploitabilidade real baixa. Fix futuro = tarball oficial SheetJS (vira build-dep do `cdn.sheetjs.com`). Comentário em `file-processor.ts`. (ultra-review 2026-05-26, Pedro escolheu "aceitar + documentar".) |

---

## Como adicionar entry

1. Escolha categoria: `H` (high), `C` (critical), `P0/P1`, `NB-` (validation findings)
2. Pegue próximo número disponível: maior `H` atual é H39, próximo é H40
3. Add linha na tabela acima
4. Add comment no código: `// H13 (review YYYY-MM-DD): <sumário curto>`
5. Commit com referência: `fix(scope): <ação> (H13)`
