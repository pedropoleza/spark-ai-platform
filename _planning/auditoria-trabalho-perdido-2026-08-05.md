# Auditoria — o que foi implementado e nunca chegou em produção

> Levantamento de 2026-08-05, 8 agentes em paralelo sobre o repositório, o banco de produção e a Vercel.
> Motivo: suspeita do Pedro de que sessões paralelas estavam deployando direto no Vercel e perdendo trabalho.

**A suspeita estava errada; o problema é real.** O Vercel já está conectado ao GitHub (`vercel git connect` responde "already connected"; todo deploy de produção carrega o alias `-git-main-`). O trabalho vaza por branch não-mergeada, migration aplicada à mão sem arquivo, e código que nunca virou commit.

---

## Recuperar agora

### 1. Salvar o cluster de ~410 linhas do working tree + a migration untracked 00123_activation_mode.sql (única cópia no mundo)

**Por que importa.** Não é código 'preso em branch' — é código que NÃO EXISTE em commit nenhum. `git log --all -S` para disqualify_after_final, plannedFollowUpCount, fireEventAutomationsByName, opportunity_name e distinctLocationLinks retorna ZERO. São 10 arquivos M + 00123_activation_mode.sql untracked, e a coluna agent_configs.activation_mode JÁ é lida por origin/main:src/lib/queue/queue-processor.ts:529 (gate H51 trigger_once, ativo em 8 de 29 agent_configs em prod). Um `git checkout .`, um `git stash drop` ou uma troca de branch com força apaga o DDL, a CHECK constraint e todo o trabalho do caso Marina/Bianca+Jussara de 2026-07-01. Confirmado nesta auditoria: `git status` mostra `?? supabase/migrations/00123_activation_mode.sql` e `git diff --stat HEAD` = 410 insertions em 10 arquivos.

**Como recuperar.** AGORA, antes de qualquer outra coisa: (1) `git add -A && git commit` numa branch wip/working-tree-2026-08-05 (sem push, sem review) só para tirar da zona de perda; (2) depois fatiar em PRs pequenos os itens 2, 3 e 8 desta lista. A 00123 deve ser RENOMEADA para timestamp (ex: 20260716204119_activation_mode.sql — é a version que o ledger de prod já registrou) e commitada com o DDL exato que está em prod: coluna text NOT NULL DEFAULT 'gate_ongoing' + constraint agent_configs_activation_mode_chk. Conferir o DDL real com `select pg_get_constraintdef(oid)` antes de commitar, para o arquivo descrever prod e não a memória.

**Risco.** BAIXO se for só commit numa branch WIP (nenhum deploy). O risco de NÃO fazer é total e irreversível. Cuidado ao commitar scripts/apply-alves-cury-agents.ts junto: os 3 flips locais (auto_pause_on_human_message, handoff_policy.enabled, lead_history_config.enabled → false) são a config CORRETA de prod; a versão commitada hoje re-liga os três e reintroduz o misfire do classificador 'humano assumiu'.

---

### 2. Normalização BR de telefone nos 3 pontos que gravam número no CRM (working tree, não commitado)

**Por que importa.** É o item de maior dano direto ao lead. Três edições coordenadas que só existem no disco: (a) src/lib/account-assistant/identity.ts — normalizePhone passa a tratar 11 dígitos com 3º dígito '9' como celular BR e força +55 mesmo com a location em fuso US; (b) src/lib/ai/action-executor.ts — update_field de contact.phone passa por normalizePhone(..., resolveLocationDefaultCountry(...)); (c) src/lib/queue/queue-processor.ts — syncCollectedDataToGHL idem. Em origin/main os três gravam o valor CRU e o normalizePhone ainda faz defaultCountry==='US' → +1. Resultado em produção HOJE: o telefone que a IA coleta do lead brasileiro vira +131999232306 (inválido) e o contato recém-qualificado fica sem entrega de WhatsApp/SMS. Verificado: `git show origin/main:.../queue-processor.ts | sed -n '/syncCollectedDataToGHL/,/^}/p' | grep normalizePhone` → nada.

**Como recuperar.** Re-implementar não é preciso — o código está pronto no working tree. Extrair APENAS esses 3 hunks num PR isolado (`git add -p` nos 3 arquivos), rodar tsc + build, e testar com 2 casos: número BR de 11 dígitos numa location com timezone US (deve virar +55) e número US de 10 dígitos (deve continuar +1). Não misturar com os outros hunks do queue-processor.ts (H51 Frente A) — são PRs diferentes.

**Risco.** MÉDIO-BAIXO. Não tem flag e muda comportamento de escrita no CRM. O caso limite a validar antes de mergear é o rep americano cujo lead digita 11 dígitos começando por 9 — a heurística do 3º dígito cobre celular BR (formato DDD+9XXXXXXXX), mas vale um teste com a base real de números US existentes para garantir que nenhum contato bom é reescrito. Zero migration, zero env.

---

### 3. H51 Frente A — membership durável em conversation_state (working tree, não commitado)

**Por que importa.** O bug 'responde a 1ª mensagem e morre' está resolvido só pela metade em prod. O main tem a Frente B (activation_mode/trigger_once, queue-processor.ts:521-532), mas ela depende de `conversationActive`, que em main (linhas 517-520) é derivado de last_ai_response_at / message_count — campos que só o action-executor escreve DEPOIS de uma resposta bem-sucedida. Ou seja: se a 1ª resposta falha (LLM, timeout, wallet) ou a ativação é por CONTEÚDO de mensagem, não existe membership para o trigger_once bypassar e o follow-up volta a ser barrado pelo targeting. Isso atinge diretamente os 7-8 lead-agents da frota que ativam por frase (Marina, Marcia/Five Star, Alves Cury). O hunk local grava a linha-dono (agent_id + status 'active', upsert idempotente com ignoreDuplicates) assim que o contato PASSA o gate, ANTES do trabalho de LLM. Verificado: `grep 'ignoreDuplicates|membership'` no queue-processor de origin/main → vazio.

**Como recuperar.** Extrair o hunk do working tree em src/lib/queue/queue-processor.ts (o bloco `if (!convState) { ... upsert conversation_state ... }` logo após o gate de targeting) num PR próprio, junto com o commit da 00123 renomeada (o PR depende da coluna existir — ela já existe em prod). Levar junto o diff de scripts/test-targeting-message.ts (+35 linhas com o set de targeting REAL da Marina e os 7 asserts que travam a premissa, inclusive o que documenta 'follow-up sem tag: evaluator BLOQUEIA') — hoje o main tem o trigger_once sem nenhum teste que prove por que o bypass é indispensável.

**Risco.** MÉDIO-BAIXO. É upsert idempotente e fail-soft (erro nunca bloqueia a resposta), não toca message_count/last_ai_response_at/ai_paused_at. O efeito colateral a vigiar: passa a existir linha de conversation_state para contatos que o agente ativou mas nunca respondeu — conferir se algum relatório/contador conta 'conversas ativas' por essa tabela antes de deployar.

---

### 4. Ligar o canal de alerta (ALERT_DISCORD_WEBHOOK) — toda a observabilidade está represada no banco

**Por que importa.** É o cano que faz TODOS os outros sinais chegarem em alguém. channelName() em src/app/api/cron/signals-alert/route.ts:67-77 só devolve canal se existir ALERT_TELEGRAM_BOT_TOKEN+CHAT_ID, ALERT_SLACK_WEBHOOK ou ALERT_DISCORD_WEBHOOK — nenhuma das quatro está na Vercel production (confirmado por `vercel env ls`). Com channel==='none' o handler retorna na linha 310 e explicitamente NÃO marca nada como alertado. O custo já pago está escrito no próprio código: o sinal 'inbound MUDO' acumulou 4013 ocorrências desde 18/06 sem sair do banco e o apagão de 17h de 28/07 passou despercebido. Pior: o ramo Discord foi adicionado em 28/07 justamente porque a operação já tem canal de OPS no Discord — e uma semana depois a env continua não existindo. Sem isso, dead-man heartbeat, detector de inbound mudo, sinais de wallet e de token são todos decorativos.

**Como recuperar.** Zero código. Criar o webhook no canal de OPS do Discord e setar ALERT_DISCORD_WEBHOOK em production na Vercel + redeploy. Validar no primeiro tick de 5min que uma mensagem chega e que os signals antigos passam a ser marcados como alertados. Como há 4013 ocorrências represadas, checar se o handler faz batch/limite antes de ligar, para não despejar milhares de linhas no canal no primeiro tick.

**Risco.** BAIXO no runtime (o cron já roda; a única mudança é o early-return deixar de acontecer). O risco real é ruído: ligar com a fila represada pode inundar o canal. Ligar fora do horário de pico e observar o primeiro tick.

---

### 5. sparkbot: deburr (acento-insensível) no targeting de agente lead-facing

**Por que importa.** O F9 do H45 entrou no main SÓ no filter-engine/executor.ts:563-579. Os dois pontos que decidem se o agente responde ao lead ficaram de fora: origin/main:src/lib/queue/targeting.ts:125-137 ainda faz `rule.tag.trim().toLowerCase()` e `value.trim().toLowerCase() === rule.custom_field_value.trim().toLowerCase()` (confirmado nesta auditoria), e filter-engine/text-ops.ts norm() é só trim+lower, sem NFD strip. `git grep deburr origin/main -- src/lib/queue/` = 0. Consequência: regra de ativação com acento não casa o CRM sem acento ('Líder' vs 'lider', 'São Paulo' vs 'Sao Paulo') — e o pior caso é a ativação POR FRASE, que é como Marcia/Five Star e Alves Cury ativam: divergência de acento entre a frase da regra e a do lead → targeting_skip e bot MUDO para aquele lead. Em 30 dias o execution_log tem 1.175 targeting_skip; parte disso pode ser exatamente isto.

**Como recuperar.** PRIMEIRO medir: `SELECT id, targeting_rules FROM agent_configs WHERE targeting_rules::text ~ '[À-ÿ]'` — se houver regra acentuada ativa na frota, o impacto sobe de MÉDIO para ALTO e vira urgência. Depois, cherry-pick cirúrgico do commit da branch sparkbot que toca src/lib/queue/targeting.ts e src/lib/account-assistant/filter-engine/text-ops.ts (só esses 2 arquivos — NÃO trazer o commit inteiro), levando junto as +7 regressões de scripts/test-targeting-message.ts (43→50) que cobrem exatamente isso. Zero migration.

**Risco.** MÉDIO. É mudança de comportamento VIVO e sem flag: afrouxa quem casa a regra de ativação da frota inteira. A direção é segura (só ignora acento, não afrouxa nada mais), mas antes de deployar rodar o gate contra uma amostra real de contatos por location e comparar o antes/depois de quem passa — um agente com regra acentuada pode começar a responder a contatos que hoje ignora.

---

### 6. Ligar COHERENCE_PIPELINE_FAMILY=1 — a família pipeline_add do coherence-gate está escrita, medida e desligada

**Por que importa.** Falsa confirmação é o atrito nº1 do SparkBot documentado no estudo de uso. O padrão PIPELINE_ADD_PATTERN foi escrito em 2026-07-06 para o caso Leidy (03/07, rep 2dbd9d0a): o bot respondeu 'Feito! ✅ Leidy Eder 3T adicionada ao funil 1- Prospects, stage New Leads' com tools=[] — nenhuma escrita aconteceu. O comentário no código registra a validação empírica de 30 dias: casa 5 confirmações REAIS (satisfeitas por create/move/update_opportunity) + a 1 falsa, com falso-positivo observado = 0. Mesmo assim isPipelineFamilyEnabled() em src/lib/account-assistant/core/coherence-gate.ts:146 exige a env, e activeClaimPatterns():150 devolve só CLAIM_PATTERNS sem ela. A env não existe na Vercel. Padrão escrito, medido, comentado e nunca ligado.

**Como recuperar.** Setar COHERENCE_PIPELINE_FAMILY=1 em production + redeploy. Acompanhar por 72h a contagem de bloqueios do gate no execution_log e conferir manualmente as primeiras ocorrências: se alguma for confirmação legítima barrada, desligar (é 1 env, rollback instantâneo).

**Risco.** BAIXO-MÉDIO. O modo de falha é o gate barrar uma confirmação verdadeira e o bot ficar estranho num turno; a medição de 30 dias diz falso-positivo = 0. Reversível em 1 flip. Ligar junto com o canal de alerta (item 4) para enxergar o efeito.

---

### 7. sparkbot: schedule_reminder capturando contact_id/contact_name no task_payload

**Por que importa.** É um cano ligado nos dois lados e desconectado no meio. origin/main:src/lib/account-assistant/proactive/reminder-runner.ts:296 e :326 JÁ leem `task.task_payload?.contact_id` e propagam para a metadata da entrega, que vira o bloco 'CONTATO EM CONTEXTO' do H45/F1-F3 no turno seguinte. Mas o insert de schedule_reminder em origin/main:src/lib/account-assistant/tools/reminders.ts (linha ~179, confirmado nesta auditoria) grava só { message, title, source, test_session_id } — sem contact_id. Efeito para o rep: 'me lembra de ligar pra Fernanda às 15h' → o lembrete dispara sem saber quem é e o bot re-busca ou re-pergunta o contato, que é exatamente a dor do H45/H47 que a frota mais reclama. É o follow-up F10 do H45 que ficou para trás.

**Como recuperar.** Mudança pequena e isolada: cherry-pick do hunk da branch sparkbot em src/lib/account-assistant/tools/reminders.ts (só esse arquivo). Não tem migration, não tem flag, não tem env — task_payload é jsonb e o leitor já tolera ausência do campo. Validar com um lembrete real: agendar via chat citando um contato e conferir que o proativo chega com o bloco de contato em foco.

**Risco.** BAIXO. Aditivo em jsonb, o consumidor já existe e trata null. Único cuidado: garantir que o contact_id gravado é o do contato DISCUTIDO (chave contact_id) e não o ghl_contact_id do card do próprio rep — a distinção anti-alucinação do H45.

---

### 8. Desqualificação automática após o follow-up final + ação 'Criar no funil' (working tree, não commitado)

**Por que importa.** Duas peças de automação que o admin configura na UI e que NUNCA disparam hoje. (a) src/lib/queue/follow-up-scheduler.ts ganha disqualifyAfterFinalFollowUp() + plannedFollowUpCount() e src/types/agent.ts ganha FollowUpConfig.disqualify_after_final (opt-in, default OFF): marca conversation_state.status='disqualified' quando o último toque sai sem resposta e dispara as automações de evento 'disqualified'. Em origin/main isso não existe de forma nenhuma — 'disqualified' só chega pelo conversation_status que o LLM devolve, e as automações de evento só rodam no queue-processor (POST-LLM, exige um inbound). Ou seja: a automação 'Lead desqualificado' que o admin configurou nunca dispara para o lead que simplesmente sumiu, que é exatamente o caso que ela existe para cobrir. (b) src/lib/ai/reaction-engine.ts ganha a ação create_opportunity (move se já existe opp na pipeline, senão cria) + a opção na UI em agent-detail-view.tsx: o H62 (commit af00320, já em main) refez o lado do GATILHO como trigger.kind='agent_activated', mas NÃO levou a AÇÃO junto — ACTION_OPTS do main tem só move_pipeline, que PULA quando o contato não tem opp.

**Como recuperar.** Dois PRs separados a partir do working tree. PR A: follow-up-scheduler.ts + types/agent.ts (a flag disqualify_after_final nasce OFF por agente — ligar depois num agente piloto). PR B: reaction-engine.ts + types/agent.ts (AutomationAction.type + opportunity_name) + agent-detail-view.tsx. No PR B, DESCARTAR o ai_activated/fireEventAutomationsByName local (redundante com o H62 já em main) e verificar se o ramo de desativação faz sentido — agent_deactivated não existe em main.

**Risco.** PR A: BAIXO (opt-in, default OFF; nada muda até alguém ligar por agente). PR B: MÉDIO — create_opportunity ESCREVE no CRM do cliente; testar numa location de teste antes, e conferir a dedup (não criar segunda opp quando já existe uma na mesma pipeline). Conflito esperado em reaction-engine.ts e agent-detail-view.tsx porque o H62 mexeu na região.

---

### 9. Duas entregas ao rep que estão prontas e mudas por falta de 1 env cada: MARINA_DAILY_PHONE e BULK_PAUSED_REMINDER_ENABLED

**Por que importa.** (a) O cron marina-daily-appointments está declarado em vercel.json ('0 12 * * *') e dispara TODO dia há ~5 semanas — e a primeira linha de runMarinaDailyDigest (src/lib/account-assistant/marina-daily.ts:83-85) retorna {ok:false, skipped:'no_phone_configured'} porque MARINA_DAILY_PHONE não existe na Vercel. Todo o resto do caminho está pronto e destravado (o digest sai por SparkZap; SPARK_OS_WA_URL/TOKEN estão setados). Falta o telefone. O retorno é ok=false silencioso em log, então nada reclama. (b) O Check 4 do bulk-runner-health-check.ts:26-35 detecta job de disparo PAUSADO há >24h com destinatários pendentes — o caso Jussara, job pausado 7 dias com 5 pendentes esquecidos — mas o aviso ao rep está atrás de BULK_PAUSED_REMINDER_ENABLED, que não existe. E o fallback (admin_signal) também não sai de lugar nenhum, porque nenhuma ALERT_* está configurada. Composto: um disparo pode ficar pausado indefinidamente e NINGUÉM é avisado, nem rep nem admin.

**Como recuperar.** Duas envs na Vercel production + redeploy. Para (a): confirmar o número da Marina com o Pedro e setar MARINA_DAILY_PHONE (formato E.164). Para (b): setar BULK_PAUSED_REMINDER_ENABLED=1 DEPOIS de ligar o canal de alerta (item 4) e de conferir por query quantos jobs pausados >24h com pendentes existem hoje — o gate original era um cuidado legítimo (evitar avalanche retroativa no 1º tick), então validar o volume antes.

**Risco.** BAIXO em (a) — a pior falha é um digest chegar no número errado; validar com dryRun antes. MÉDIO em (b) apenas no primeiro tick: se houver muitos jobs pausados antigos, vários reps recebem aviso de uma vez. Rodar a query de contagem antes de flipar.

---

### 10. H29/H43 — religar a camada TurnContext (hoje é código morto que a documentação vende como 'DEPLOYADA')

**Por que importa.** Confirmado nesta auditoria com grep direto: autoRegisterFromToolResult, registerEntity, registerSearch, registerWrite, recordQuestion, recordBulkChoice, rankCandidates e detectRecoveryPlan NÃO TÊM NENHUM CONSUMIDOR em origin/main fora do próprio diretório conversational/. processor.ts:587 cria o estado VAZIO e :643 renderiza esse estado vazio ANTES de qualquer tool rodar; renderTurnContextForPrompt (turn-context.ts:380) curto-circuita com estado virgem e devolve string vazia. Portanto a seção '# CONTEXTO FRESCO DO TURN (entidades já resolvidas — NÃO re-pergunte)' NUNCA é emitida em produção, e com ela caem juntos: o detector de rajada determinístico que o CLAUDE.md lista como 'H43 Onda 1 DEPLOYADA' (consecutive_writes, que só seria incrementado por registerWrite), o loop-detection do caso Gustavo (repeated_questions/recordQuestion) e o bulk_session_state. Isso é exatamente a dor que H45 e H47 continuam perseguindo: o H45 cobre SÓ contato (getActiveContactContext em processor.ts:684), enquanto opportunity, stage, appointment, calendar, task, note e last_search seguem sendo re-descobertos a cada tool.

**Como recuperar.** Não é cherry-pick — é re-implementação, e o desenho atual tem um defeito estrutural: o bloco é renderizado UMA vez, na montagem do prompt, antes do loop de tools, então mesmo populando durante o turno o texto já foi gerado. Onda dedicada em 2 passos: (1) chamar autoRegisterFromToolResult dentro do loop de tools em core/run-sparkbot-turn.ts, gravando no mesmo TurnContextState; (2) re-renderizar o bloco a cada iteração do loop (ou anexá-lo ao tool_result), atrás de uma flag TURN_CONTEXT_ENABLED default OFF, com eval supervisionado em 1 conversa real antes de ligar. Enquanto isso, corrigir DECISIONS.md H29 e a seção H43 do CLAUDE.md, que hoje afirmam o oposto do código.

**Risco.** MÉDIO-ALTO — por isso está por último apesar do impacto alto. Mexe no loop de tools do SparkBot, o caminho mais quente do produto, e injeta texto novo no prompt a cada iteração (custo de token e risco de quebrar o cache que o H44/Onda B comprou). Obrigatório: flag OFF, medir tokens antes/depois, e validar contra o caso Gustavo antes de ligar para a frota.

---

## Não recuperar

- **sparkbot: supabase/migrations/00117_append_recent_contact.sql (RPC do ring buffer de contatos)** — REIMPLEMENTADO MELHOR NO MAIN. origin/main:supabase/migrations/00121_recent_contacts_rpc.sql (H47-F1, 2026-07-10) cria a MESMA função public.append_recent_contact(uuid,jsonb,int) com a mesma semântica, e active-contact.ts:132 já a chama com fallback PGRST202. A versão do main tem `security definer`, `set search_path = public` e `grant execute to service_role`; a da branch não tem nenhum dos três. Aplicar a 00117 da branch por cima faria CREATE OR REPLACE na função viva em prod e a trocaria por uma mais fraca — é downgrade silencioso. DESCARTAR o arquivo, não renumerar.

- **sparkbot: H58 slot-guard (src/lib/ai/slot-guard.ts) + scripts/setup-alves-cury-v2.ts e stress-alves-cury-v2.ts** — JÁ ESTÁ NO MAIN e o main está À FRENTE. slot-guard.ts entrou por 29a0795 e desde então ganhou isSameSlotInstant (H61, caso Adriana) e coerceStartTimeToTimezone (H66, offset errado do LLM), que a branch não tem. Os dois scripts do Alves Cury são byte-idênticos entre branch e main (`git diff --stat` sai vazio). Na resolução dos conflitos de action-executor.ts / queue-processor.ts / slot-guard.ts, resolver SEMPRE a favor do main — a branch é subconjunto estrito e 'pegar o lado da branch' reverteria H61 e H66 em produção.

- **Merge da branch sparkbot inteira (H46 Campanhas em Grupo V2 + rep_media, 23 commits)** — RISCO MAIOR QUE BENEFÍCIO AGORA. Base 100 commits atrás do main, 9 conflitos de conteúdo reais, 7 migrations com número colidido, e o bloco de maior valor (H46) está 100% atrás de flags que ninguém vai ligar tão cedo — a arquitetura V1 que ele substitui (H40) sequer chegou a rodar em prod. Zero usuário está perdendo função hoje. Tratar como backlog: quando houver decisão de vender campanha de grupo, RE-EXTRAIR F1→F2→F3 em 3 PRs com migrations renumeradas por timestamp e as flags GROUP_CAMPAIGNS_V2/REP_MEDIA_ENABLED OFF, nunca `git merge`. Atenção ao mergear o bulk-message-runner: a reescrita NÃO está atrás de flag.

- **H40 Campanhas em Grupo V1 (no main, GROUP_CAMPAIGNS_ENABLED) — não ligar, ARQUIVAR** — A receita de ativação documentada em CLAUDE.md:214-218 é impossível: o caminho de envio (bulk-message-runner.ts:827 sendToGroup → getStevoInstanceForRep exigindo kind='dedicated' → sendGroupText POST {serverUrl}/send/text) bate direto na API do Stevo, que foi descomissionado no cutover do H57 em 03/08. Prod tem 1 linha em stevo_instances e ZERO com kind='dedicated'. Ligar a env hoje faz a campanha falhar no envio. Ação: registrar entrada H46 em DECISIONS.md marcando a arquitetura do H40 como superada, e remover ou marcar a seção do CLAUDE.md.

- **feat/sparkbot-assistente-humano: src/lib/ai/model-params.ts (claudeSamplingParams / isNextGenModel)** — PREMISSA REFUTADA + RISCO ASSIMÉTRICO. O commit (02/07) assume que Sonnet 5 dá 400 com temperature; origin/main:src/lib/billing/pricing.ts:27-31 registra 3 agentes lead-facing rodando claude-sonnet-5 por 3 semanas pelo caminho processWithClaude, que manda temperature: 0.8 — logo as chamadas passam. O ganho real seria só thinking:{type:"disabled"}. O risco não é simétrico: o patch entra em openai-client.ts:531 num call site SEM try/catch, e agente lead-facing não tem fallback cross-provider (só o SparkBot tem) — se a API recusar o campo, o lead fica a seco. Além disso os 3 call sites mudaram de linha no main e o patch não aplica sozinho.

- **feat/sparkbot-assistente-humano: rep_notes (tools note_to_self/list/search/delete + REP_NOTES_ENABLED)** — NÃO LIGAR AGORA, MAS VERSIONAR O SQL. O schema já está VIVO em prod desde 02/07 (tabela rep_notes com índice ivfflat, search_rep_notes, purge_old_rep_notes e o cron sparkbot-cleanup reescrito pelo 00120) — os arquivos 00119/00120 só existem na branch. O código nunca foi deployado e o F1.6 (smoke com o LLM dirigindo as tools) NUNCA foi feito. Prioridade: commitar os DOIS .sql (renumerados por timestamp) para o repo descrever o que prod tem, e deixar o código com a flag OFF para uma onda dedicada. Hoje é uma purga LGPD rodando diariamente sobre uma tabela que ninguém escreve.

- **Ligar AGENT_MOTOR_UNIFIED (Plataforma Modular H35)** — ZERO GANHO DE USUÁRIO POR CONSTRUÇÃO. Com a flag OFF o processor usa o builder legado; os testes de paridade exigem output byte-idêntico, então ligar não muda nada para rep nem lead — só troca o caminho de código, que nunca foi exercitado em prod. O CLAUDE.md manda explicitamente validar 1 conversa real antes. É item de decisão de roadmap (habilitar agentes custom compostos), não recuperação de trabalho perdido.

- **Merge cego da branch fix/sparkbot-multi-location-loop (1 commit, 3afebfe)** — CORRETO NO DIAGNÓSTICO, DEFASADO NA IMPLEMENTAÇÃO, E BLAST RADIUS ZERO HOJE. Prod tem 0 de 245 reps com location duplicada em ghl_users e a rep do caso (Alexandre Padilha) já foi corrigida à mão. Pior: o fix da branch casa a escolha do picker com `u.location_id.toLowerCase() === pick` exato, e desde o H47-F2 (2026-07-10) o texto do tap chega com `[opção escolhida na lista: "..."]` grudado — mergear como está NÃO conserta o loop pela rota real de WhatsApp. E o hunk de prompt-builder.ts conflita com o H54/Onda B, que MOVEU o bloco pro runtime context; aceitar o lado da branch desfaz ~$50/mês de cache e quebra test-motor-parity. Re-implementar depois (é pequeno: distinctLocationLinks + stripOptionEcho antes da comparação + cobrir tools/identity.ts:121,166), não mergear.

- **Os 10 dangling commits do fsck, os 8 stashes dropados e os 6 worktrees** — NADA A RECUPERAR — VERIFICADO UM A UM. Todo o conteúdo dos 10 dangling commits já está em origin/main por símbolo (4663b90→deferBlockedTask/isHumanProofMsg; e390fce/f877c31→deliveredWhatsapp/book_blocked_no_contact; d18924e→isSameSlotInstant/idempotent_noop; d23e68b/f575798→superados por c4a353e, booking-guard.ts está em main; a925bf9→duplicata de 2bfd419; 600e770→manual_context). Os 6 worktrees (4 em .claude/worktrees + osaka + missoula) têm working tree LIMPO e todos os HEADs são ancestrais de origin/main, exceto o da branch sparkbot já catalogada. Não gastar tempo aqui.

- **H30 — ERROR_RECOVERY_MAP e 'Disambiguation by Elimination' (código morto no main)** — PROMETIDO COMO EXECUTÁVEL, MAS NUNCA FOI. rankCandidates, disambiguate, renderDisambiguationOptions, detectRecoveryPlan e ERROR_RECOVERY_MAP não têm nenhum consumidor fora de conversational/. O que chega ao prompt é só o texto estático ERROR_RECOVERY_PROMPT_GUIDE (prompt-builder.ts:824). Diferente do TurnContext (que resolve dor viva de re-pergunta), aqui o comportamento por prompt já cobre razoavelmente e ligar recovery automático por score é mudança de comportamento arriscada sem eval. Decidir: apagar as ~127 linhas de disambiguation.ts + o mapa e corrigir a entrada do DECISIONS.md, ou abrir onda dedicada. Não recuperar às pressas.


## Higiene de deploy

- NUMERAÇÃO DE MIGRATION: abandonar o sequencial NNNNN_. Passar a nomear por timestamp UTC (YYYYMMDDHHMMSS_slug.sql), que é o formato que o próprio ledger do Supabase usa. Motivo concreto: hoje 00117, 00118, 00119, 00120, 00121, 00122, 00123 e 00127 estão cada um com 2 ou 3 conteúdos DIFERENTES disputando o mesmo número entre origin/main, a branch sparkbot, a branch feat/sparkbot-assistente-humano, a branch origin/claude/sales-ai-healthcheck-messages-b706gu e o working tree. Colisão de número não gera conflito de git (nomes de arquivo diferem) — mergeia em silêncio e deixa duas versões da mesma versão no repo. Com timestamp isso é impossível por construção.

- REGRA DURA 'APLICOU = COMMITOU NO MESMO DIA': proibir apply_migration via MCP sem que o arquivo .sql entre num PR no mesmo dia. Hoje há 3 classes de dívida por causa disso: (a) DDL vivo em prod sem arquivo em NENHUMA ref (get_onboarding_signals, ledger 20260622015037; e o job pg_cron followup-runner, que entrega TODO o H33/H41, criado à mão no banco); (b) DDL só no working tree (00123_activation_mode.sql, com queue-processor.ts:529 de PRODUÇÃO já lendo a coluna); (c) DDL só numa branch não mergeada (rep_notes 00119/00120, entry_by_automation 00127). Ação imediata: extrair os órfãos com pg_get_functiondef / SELECT command FROM cron.job e escrever os arquivos.

- SCRIPT DE RECONCILIAÇÃO NO CI: criar scripts/check-migration-drift.ts que cruza supabase_migrations.schema_migrations (do projeto vyfkpdnwevtuxauacouj) com ls supabase/migrations/ e falha o build em 3 condições: (1) version no ledger sem arquivo correspondente; (2) arquivo com número duplicado; (3) version registrada com formato curto. Corrigir também as 6 linhas já ruins do ledger: 00124..00129 estão gravadas com version curta ('00124') e statements IS NULL — como version é texto, '00124' ordena ANTES de '20260406150932' (create_locations), então qualquer replay guiado pelo ledger tentaria criar wallet_block antes das tabelas existirem.

- PROIBIR PUSH DIRETO EM main: proteger o branch no GitHub e exigir PR com CI verde (tsc + next build + os testes de paridade: test-motor-parity, test-sales-parity, test-targeting-message, test-weekday-guard, test-override-gate). O anti-pattern 'push sem conferir deploy' já está no CLAUDE.md e voltou a acontecer; transformar em automação: hook de post-push que roda `npx vercel ls --prod` em loop até o commit aparecer Ready e falha alto se der Error.

- NENHUM WORKING TREE COMO ÚNICA CÓPIA: hoje ~410 linhas em 10 arquivos (caso Marina/Bianca + Jussara, 2026-07-01) existem SÓ no disco do Pedro — `git log --all -S` para disqualify_after_final, plannedFollowUpCount, fireEventAutomationsByName, opportunity_name retorna ZERO commits. Um `git checkout .` apaga tudo. Adotar: hook de fim-de-sessão que faz `git add -A && git commit` numa branch wip/<data> (mesmo sem push) sempre que houver diff, e um alerta se existir diff não commitado com mais de 48h.

- BRANCH LONGA É DÍVIDA, NÃO ATIVO: regra de que branch de feature rebasa com main toda semana e nunca passa de ~15 commits. A branch sparkbot tem 23 commits e está 100 commits ATRÁS do main; resolver o conflito dela em action-executor.ts / queue-processor.ts 'pegando o lado da branch' REVERTERIA H61 (booking idempotente, caso Adriana) e H66 (coerceStartTimeToTimezone) em produção. Quando a distância passar desse ponto, a política é RE-EXTRAIR o comportamento em PRs pequenos, nunca `git merge`.

- INVENTÁRIO ÚNICO DE FLAGS: hoje /api/admin/cron-health mostra 8 envs de ~40 e o .env.example documenta ~12. As que estão apagando função (todas as ALERT_*, COHERENCE_PIPELINE_FAMILY, BULK_PAUSED_REMINDER_ENABLED, MARINA_DAILY_PHONE, PROACTIVE_EVENTS_LOCATIONS, STRICT_CLAUDE_ONLY, INTERNAL_TEAM_PHONES) não aparecem em lugar nenhum. Criar src/lib/config/flags.ts como fonte única (nome, default, o que liga/desliga, dono, data de criação), gerar o painel e o .env.example a partir dele, e exibir o valor EFETIVO — lembrando que várias têm lógica invertida e estão ON por omissão (BLOCKED_SLOTS_CONTEXT_ENABLED, CLAUDE_STRUCTURED_OUTPUT, INBOUND_WEBHOOK_CAPTURE).

- TODA FLAG NASCE COM DATA DE VALIDADE: no PR que introduz uma flag OFF, obrigar um campo 'decidir até <data>' (flip ou arquivar). Flag OFF há mais de 60 dias vira item de revisão automático. Sem isso repetimos H40 (43 testes verdes, 2 migrations aplicadas em prod, arquitetura invalidada pelo H57 e ninguém arquivou), AGENT_MOTOR_UNIFIED e o gate do bulk pausado.

- ENV MORTA SAI DO PAINEL: AGENT_ENTITLEMENTS_ENFORCED é lida com `return false` hard-coded em entitlements.ts:29-33 (leitura do process.env comentada), mas /api/admin/cron-health:28 e /hub/admin/health ainda a exibem como se decidisse algo. Remover do painel e do CLAUDE.md (CLAUDE.md:172 e :176 ainda descrevem o gate como ativo três linhas depois de registrar a reversão).

- DOCS COM MESMO PESO DE CÓDIGO NO PR: docs/DECISIONS.md não tem entrada para H46, H48, H49, H50, H51, H56 e H59 — inclusive para o H50, que tem seção inteira em CLAUDE.md marcada 'NO AR' e está de fato implementado. E a doc contradiz o código em pontos perigosos: CLAUDE.md:100 manda fazer rollback com SPARKBOT_WA_TRANSPORT=stevo, mas o Stevo foi deletado no cutover — esse rollback hoje CALA o SparkBot em vez de restaurar. CLAUDE.md:221 diz TASK_ORCHESTRATOR_ENABLED 'flag OFF' e :232 diz 'JÁ LIGADA'. RUNBOOK.md:236-241 lista 3 crons e prod roda 10, incluindo um /api/cron/summary-notes que não existe. Checklist de PR: se mudou comportamento, atualizou DECISIONS.md + a seção correspondente do CLAUDE.md + RUNBOOK.

- VALIDAR STAGING FRESCO A CADA RELEASE: criar um branch Supabase do zero aplicando SÓ os arquivos versionados e rodar smoke. Hoje isso reprovaria em silêncio — sem 00123_activation_mode.sql a coluna agent_configs.activation_mode não existe, o cast opcional em queue-processor.ts:529 devolve undefined, todos os agentes caem em gate_ongoing e o bug 'responde a 1ª e morre' volta SEM erro nenhum. Cuidado ao usar `supabase db push` contra PROD: ~120 versions de arquivo estão fora do ledger (foram aplicadas via MCP sem registrar) e ele tentaria reaplicar tudo desde 00001, incluindo as migrações de DADOS 00058/00059/00060 e o UPDATE/DELETE do 00128_followup_hygiene.

- CORRIGIR O CRON QUE APONTA PRA PROD: as migrations 00070_pg_cron_parametrize_url e 00104_pg_cron_reactive_poll_due_guard nunca rodaram — os jobs vivos sparkbot-proactive, followup-runner e process-message-queue ainda hardcodam https://spark-ai-platform.vercel.app e o Bearer em texto puro, em vez de ler public.cron_config. Consequência: qualquer branch/fork do Supabase herda um cron martelando o endpoint de PRODUÇÃO a cada 30s, e o due-guard de economia do H44 nunca existiu. Aplicar as duas e rotacionar o secret que está exposto em cron.job.command.
